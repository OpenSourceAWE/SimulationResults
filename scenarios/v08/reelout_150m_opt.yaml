# Run summary for output/reelout_150m_opt.arrow, written by examples/simple_reelout.jl at the end of the run.
success_criteria: "all 9 passed"    # pass/fail verdict vs V3Kite's success criteria
simulation:
  script: "simple_opt_reelout.jl"   # script that produced this run
  project: "system_reelout_150m.yaml" # system project flown
  rel_turbulence: 0.0               # turbulence level in [0, 1] passed to init
  wind_speed: 8.0                   # mean wind speed in m/s passed to init
  time: "18:02:51"                  # wall-clock time the run finished
  date: "2026-08-22"                # wall-clock date the run finished
  hostname: "ufryzen"               # machine the run executed on
  git_hash: "c29158b"               # SimpleKiteControllers.jl commit hash
  git_status: "dirty"               # SimpleKiteControllers.jl working tree: clean or dirty
fig8_metrics:
  settled_from_s: 28.7              # sim time the scoring window begins [s]
  settle_time_s: 26.7               # time after t_start to converge [s]
  laps: 7.5                         # figure-eight laps completed
  cross_track_deg:
    rms: 1.53                       # RMS cross-track error [deg]
    mean: 1.18                      # mean cross-track error [deg]
    max: 4.99                       # max cross-track error [deg]
  elevation_deg:
    min_settled: 9.6                # min elevation, settled window [deg]
    min_whole_run: 9.6              # min elevation, whole run [deg]
  peak_turn_rate_deg_s: 128         # peak heading rate [deg/s]
  extent:
    azimuth_deg:
      min: -15.2                    # flown azimuth reach, negative side [deg]
      max: 14.1                     # flown azimuth reach, positive side [deg]
    azimuth_pct_of_amplitude:
      min: 101                      # negative reach as % of the pattern's ±A
      max: 99                       # positive reach as % of the pattern's ±A
    worst_lobe_deg:
      min: -9.2                     # weakest lobe, negative side [deg]
      max: 9.0                      # weakest lobe, positive side [deg]
    elevation_span_deg: 20.7        # flown elevation span over the whole settled window — the pattern DESCENDS as the tether grows, so this is mostly that descent [deg]
    elevation_span_lap_deg: 8.1     # flown elevation span per lobe, which is the pattern's own height [deg]
    elevation_span_pct_of_b: 95     # flown span as % of the B commanded where it was flown
  tether_force_N:
    mean: 6540                      # mean tether force, settled window [N]
    std: 297                        # std tether force, settled window [N]
    cv_pct: 4.5                     # coefficient of variation [%]
  steering:
    peak_abs_u_s: 0.32              # peak |rel_steering| commanded
    pct_time_within_2pct_of_peak: 2 # % of time within 2% of peak (saturation)
    hf_std_steering: 0.0057         # high-frequency std of steering (chatter)
    hf_std_turnrate_deg_s: 0.88     # high-frequency std of heading rate [deg/s]
  tape:
    delivered_peak_abs_u_s: 0.286   # peak steering the KCU tape delivered
    commanded_peak_abs_u_s: 0.32    # peak steering commanded
    rate_limited_pct_time: 7        # % of time the tape's rate limit was hit
    rate_limited_peak_per_s: 0.2    # peak tape rate reached [1/s]
    rate_limit_per_s: 0.2           # KCU's configured rate limit [1/s]
reelout:
  v_app_phase4:
    duration_s: 68.4                # phase-4 window length [s]
    mean_m_s: 29.53                 # mean apparent wind speed [m/s]
    min_m_s: 25.53                  # min apparent wind speed [m/s]
    max_m_s: 31.56                  # max apparent wind speed [m/s]
    v_app_ref_m_s: 27.0             # reference apparent wind speed [m/s]
    deviation_pct: 9.4              # mean v_app deviation from v_app_ref [%]
  tether:
    start_m: 150.0                  # tether length at run start [m]
    end_m: 380.0                    # tether length at run end [m]
    target_m: 380.0                 # reelout_l_max target [m]
    stop_reason: "length"           # criterion that ended reel-out: length, laps, or none
    laps_reeled: 7.72               # figure-eight laps completed by the time reel-out ended
  force:
    cf_force_ro: 1.05               # crest factor: peak / mean tether force over the reeling window
  power:
    cf_power_ro: 1.13               # crest factor: peak / mean reel-out power over the reeling window
    duration_s: 70.8                # reeling window length [s]
    n_samples: 6368                 # sample count in the reeling window
    energy_kJ: 1502.0               # energy over the reeling window [kJ]
    energy_run_kJ: 1498.9           # energy over the whole run [kJ]
  winch_state:
    speed_pct: 100.0                # % of the reeling window in speed control (state 1)
    lower_force_pct: 0.0            # % in the LowerForceController, reeling in (state 0)
    upper_force_pct: 0.0            # % in the UpperForceController, force capped at f_high (state 2)
    n_samples: 6368                 # sample count in the reeling window
  ringing:
    n_peaks: 7                      # ring peaks detected above peak_floor
    period_s: 2.57                  # mean peak-to-peak ring period [s]
    zeta: 0.087                     # damping ratio from the peak log decrement
    overshoot_m_s: 0.69             # first ring peak's amplitude above the local trend [m/s]
    duration_s: 20.0                # time until the ring decays below settle_frac of overshoot_m_s [s]
    peak_v_reelout_m_s: 3.56        # raw v_reelout max within ring_span [m/s]
    steady_v_reelout_m_s: 3.32      # mean v_reelout after ring_span [m/s]
traj_opt:
  power:
    predicted_W: 21727              # predicted mean reel-out power of the paths actually flown, weighted by their share of the reeling window [W]
    predicted_initial_W: 19034      # predicted mean reel-out power of the path installed before the run [W]
    predicted_paths:
      t_000.0_s: 19034              # 19% of the reeling window
      t_040.2_s: 21147              # 15% of the reeling window
      t_050.7_s: 22059              # 15% of the reeling window
      t_061.5_s: 22599              # 15% of the reeling window
      t_072.4_s: 22942              # 34% of the reeling window
      t_096.5_s: 23155              # 1% of the reeling window
    measured_W: 21228               # mean reel-out power the run harvested [W]
    ratio: 0.98                     # measured / predicted, against the weighted prediction
  guess:
    a_deg: 30.0                     # width of the guess lemniscate; azimuth spans ±a [deg]
    b_deg: 12.0                     # height of the guess, peak to peak [deg]
    el_center_deg: 29.0             # centre elevation of the guess [deg]
    points: 361                     # points the guess was sent with
    depower_seed_m: 1.85            # power-tape length the solve started from, input_depower ramped with the wind above input_depower_wind_ref; only a seed, the server optimizes it [m]
    depower_optimized:
      t_000.0_s: 1.422              # optimizer's l_dp [m]; rel_depower equivalent 0.271 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_040.2_s: 1.418              # optimizer's l_dp [m]; rel_depower equivalent 0.271 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_050.7_s: 1.425              # optimizer's l_dp [m]; rel_depower equivalent 0.272 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_050.7_s_2: 1.415            # optimizer's l_dp [m]; rel_depower equivalent 0.270 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_061.5_s: 1.413              # optimizer's l_dp [m]; rel_depower equivalent 0.270 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_072.4_s: 1.411              # optimizer's l_dp [m]; rel_depower equivalent 0.269 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_096.5_s: 1.413              # optimizer's l_dp [m]; rel_depower equivalent 0.270 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_105.9_s: 1.416              # optimizer's l_dp [m]; rel_depower equivalent 0.270 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
  path:
    points: 360                     # points of the path installed before the run
    points_final: 360               # points of the path at the end; differs when reopt installed one
    az_center_deg: 0.5              # centre azimuth of the path [deg]
    az_amplitude_deg: 19.7          # half-width of the STARTUP path; the criteria are scored against the pattern commanded at each lap, whose mean is lift_budget's reference [deg]
    el_center_deg: 23.5             # centre elevation of the path [deg]
    el_height_deg: 13.3             # elevation span of the path, peak to peak [deg]
    downloops: true                 # traversal direction the optimizer solved for
  feasibility:
    min_required: 0.82              # min_feasibility_margin of data/traj_opt.yaml [-]
    turn_radius_headroom: 1.2       # factor on the gate's number for what the GATE adds: its finite-difference curvature estimate and the elevation lift [-]
    turn_radius_request_m: 15.82    # minimum turn radius asked of the optimizer for the LAST request; the gate's margin/(c1*max_steering) times the headroom and the lap's reel-out ratio, since the optimizer measures the radius up the reel-out while the run flies the curve at the anchor [m]
    turn_radius_scale: 1.311        # the two corrections together, as sent [-]
    turn_radius_lap_reelout_m: 30.0 # reel-out per lap ASSUMED for the startup request, the only one with no reply to measure it off [m]
    margin_start: 0.9               # curvature margin at the starting length; the worst case only when one path is flown throughout — see the docstring [-]
    margin_end: 2.29                # curvature margin at reelout_l_max [-]
    c1_final: 0.2133                # turn-rate gain at depower_final [1/m]
    margin_final: 1.73              # curvature margin phase 5 flies with: depower_final's c1, at reelout_l_max, on the STARTING path lifted by el_offset_final [-]
    margin_final_flown: 0.9         # the same, for the last path the re-optimizer installed — the one phase 5 actually inherited [-]
  reopt:
    enabled: true                   # re-optimization during the run
    warm_start: true                # /step alone, seeded from the previous optimum; false re-inits from the parametric guess at every length
    requests: 6                     # solves that completed, accepted or rejected
    blocking: true                  # simulation held while a solve ran
    blocked_s: 44.2                 # wall time the simulation was frozen waiting for replies [s]
    installed: 6                    # new paths actually flown
    blend_retries: 1                # cold-restart attempts spent on a rejected reply — a folded blend, a collapsed prediction, or a clearance/elevation shortfall re-asked at a raised floor — across every install this run
    events:
      t_040.2_s: "installed — margin 1.05 (phase 5: 1.62 at 380 m), clearance 47.7 m, 21147 W predicted" # at L = 191 m
      t_050.7_s: "installed — margin 1.02 (phase 5: 1.32 at 380 m), clearance 51.0 m, 22059 W predicted" # at L = 226 m
      t_061.5_s: "installed — margin 0.99 (phase 5: 1.11 at 380 m), clearance 57.3 m, 22599 W predicted" # at L = 262 m
      t_072.4_s: "installed — margin 0.87 (phase 5: 0.86 at 380 m), clearance 65.7 m, 22942 W predicted" # at L = 298 m
      t_096.5_s: "installed — margin 0.94 (droop spread at 25 %, lobe lift at 0 %) (phase 5: 0.73 at 380 m), clearance 88.2 m, 23155 W predicted" # at L = 378 m
      t_105.9_s: "installed — margin 0.90 (droop spread at 0 %, lobe lift at 0 %) (phase 5: 0.90 at 380 m), clearance 89.6 m, 23015 W predicted" # at L = 380 m
  el_bias:
    gain: 0.5                       # per-lap learning gain for the elevation bias; 0 = off
    final_deg: 1.38                 # mean elevation correction added to the last installed path [deg]
    bins: 5                         # el_bias_bins, azimuth bands the correction is learnt over; 1 = one rigid shift
    smooth: 0.25                    # el_bias_smooth, how far each band is pulled towards its neighbours after every lap [-]
    profile_deg: [1.01, 1.16, 1.67, 1.94, 1.12] # the learnt correction per band, crossing first [deg]
    lift_deg: 1.5                   # el_offset_final, the fixed lift added on top of the learnt bias [deg]
    lift_lead_s: 8.0                # el_offset_lead, how early the lift is allowed to latch; 0 = at the end [s]
    wing_deg: 1.5                   # el_offset_wing, extra lift at the lobes, baked into every installed path [deg]
    wing_mode: "azimuth"            # el_offset_wing_mode, the coordinate that lift is shaped over
    wing_az_deg: 10.0               # azimuth mode: azimuth beyond which that lift is full; it ramps over el_offset_wing_blend below it [deg]
    wing_depth: 0.0                 # elevation mode: depth below the path's elevation centre where the lift starts, in half-spans; full at its lowest point [-]
    lift_t_s: 89.1                  # when the lift actually latched [s]
    lift_remaining_m: 26.5          # reel-out left at that moment; > 0 means the lead fired, 0 means phase 5 did [m]
    shift_delivery:
      t_044.2_s: "blended in"       # +0.41° of shift (mean over the bands), curvature margin 1.10 vs 0.82 required
      t_054.8_s: "blended in"       # +0.17° of shift (mean over the bands), curvature margin 1.01 vs 0.82 required
      t_065.5_s: "blended in"       # +0.12° of shift (mean over the bands), curvature margin 0.94 vs 0.82 required
      t_076.4_s: "blended in (100 % of the mean, 75 % of the spread)" # +0.07° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_080.5_s: "blended in"       # +0.00° of shift (mean over the bands), curvature margin 0.84 vs 0.82 required
      t_084.5_s: "blended in (100 % of the mean, 75 % of the spread)" # +0.07° of shift (mean over the bands), curvature margin 0.83 vs 0.82 required
      t_088.5_s: "blended in"       # -0.00° of shift (mean over the bands), curvature margin 0.85 vs 0.82 required
      t_092.5_s: "blended in"       # +1.50° of shift (mean over the bands), curvature margin 0.87 vs 0.82 required
      t_096.5_s: "carried by an install (25 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.94 vs 0.82 required
      t_100.6_s: "held back"        # -0.00° of shift (mean over the bands), curvature margin 0.40 vs 0.82 required
      t_105.9_s: "carried by an install (0 % of the spread)" # +0.53° of shift (mean over the bands), curvature margin 0.90 vs 0.82 required
      t_110.0_s: "held back"        # +0.00° of shift (mean over the bands), curvature margin 0.24 vs 0.82 required
    laps:
      lap_1: -0.8                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.80°, correction became [+0.25 +0.35 +0.48 +0.56 +0.42]° (crossing … lobe)
      lap_2: -0.32                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.73°, correction became [+0.21 +0.40 +0.72 +0.91 +0.65]° (crossing … lobe)
      lap_3: -0.2                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.77°, correction became [+0.16 +0.44 +0.91 +1.18 +0.81]° (crossing … lobe)
      lap_4: -0.09                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.77°, correction became [+0.12 +0.43 +1.04 +1.36 +0.91]° (crossing … lobe)
      lap_5: -0.08                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.82°, correction became [+0.15 +0.46 +1.15 +1.49 +0.98]° (crossing … lobe)
      lap_7: -0.44                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.30°, correction became [+1.01 +1.16 +1.67 +1.94 +1.12]° (crossing … lobe)
    lap_profiles:
      lap_1: "-0.46 -0.73 -0.98 -1.23 -0.72" # error vs the optimizer's curve per band, crossing first; 193/203/171/162/306 samples, correction now [+0.25 +0.35 +0.48 +0.56 +0.42]° (crossing … lobe)
      lap_2: "+0.17 -0.12 -0.53 -0.92 -0.28" # error vs the optimizer's curve per band, crossing first; 177/183/160/166/264 samples, correction now [+0.21 +0.40 +0.72 +0.91 +0.65]° (crossing … lobe)
      lap_3: "+0.22 -0.07 -0.46 -0.85 -0.04" # error vs the optimizer's curve per band, crossing first; 190/199/171/156/253 samples, correction now [+0.16 +0.44 +0.91 +1.18 +0.81]° (crossing … lobe)
      lap_4: "+0.18 +0.04 -0.38 -0.74 +0.15" # error vs the optimizer's curve per band, crossing first; 195/212/187/137/250 samples, correction now [+0.12 +0.43 +1.04 +1.36 +0.91]° (crossing … lobe)
      lap_5: "+0.06 -0.02 -0.34 -0.68 +0.27" # error vs the optimizer's curve per band, crossing first; 208/227/201/131/245 samples, correction now [+0.15 +0.46 +1.15 +1.49 +0.98]° (crossing … lobe)
      lap_7: "-0.87 -0.68 -0.58 -0.72 +0.17" # error vs the optimizer's curve per band, crossing first; 149/161/161/130/258 samples, correction now [+1.01 +1.16 +1.67 +1.94 +1.12]° (crossing … lobe)
  droop_profile:
    az_00_20_pct: 1.86              # kite below the path's elevation centre [deg], over 1441 samples; the path itself sits 0.33 half-spans down there and the kite -0.34° under it
    az_20_40_pct: 1.75              # kite below the path's elevation centre [deg], over 1563 samples; the path itself sits 0.25 half-spans down there and the kite -0.71° under it
    az_40_60_pct: 1.81              # kite below the path's elevation centre [deg], over 1388 samples; the path itself sits 0.11 half-spans down there and the kite -1.42° under it
    az_60_80_pct: 2.65              # kite below the path's elevation centre [deg], over 1145 samples; the path itself sits 0.21 half-spans down there and the kite -1.91° under it
    az_80_100_pct: 1.48             # kite below the path's elevation centre [deg], over 2006 samples; the path itself sits 0.20 half-spans down there and the kite -0.79° under it
    centre_to_lobe_deg: 0.79        # how much deeper the kite flies in its worst azimuth band than in the crossing — what a SHAPED lift is aimed at, where el_bias and el_offset_final can only move the pattern rigidly [deg]
  lift_budget:
    delivered_deg: 2.88             # mean correction the path in the air actually carries — el_bias plus el_offset_final once it has been delivered [deg]
    delivered_profile_deg: [2.88, 2.88, 2.88, 2.88, 2.88] # the same, per azimuth band, crossing first [deg]
    commanded_az_amp_deg: 15.53     # mean half-width the run was COMMANDED to fly, against the startup path's in traj_opt.path [deg]
    commanded_el_height_deg: 9.57   # mean commanded pattern height [deg]
    wing_deg: 1.5                   # el_offset_wing, the fixed lobe lift baked into every installed path [deg]
    el_min_run_deg: 9.6             # lowest elevation over the whole run — set in phase 4, so a phase-5 lift does not move it [deg]
    el_min_final_deg: 9.57          # lowest elevation in phase 5, which is what el_offset_final buys [deg]
    azimuth_reach_pos_deg: 4.52     # reach margin: flew 15.39° against the 10.87° required by min_span_frac = 0.70, +42 %
    azimuth_reach_neg_deg: 4.84     # reach margin: flew 15.72° against the 10.87° required by min_span_frac = 0.70, +45 %
    elevation_span_deg: 2.39        # reach margin: flew 9.09° against the 6.70° required by min_span_frac = 0.70, +36 %
    tightest: "elevation span"      # the size criterion with the least room, +2.39° (+36 %) — what the NEXT lift has to spend
  clearance:
    path_min_m: 43.5                # lowest point of the PRE-FLIGHT path at the starting length [m]
    flown_min_m: 45.8               # lowest point the kite actually reached over the run [m]
    min_required_m: 40.0            # min_height of data/traj_opt.yaml [m]
    elevation_min_asked_deg: 9.5    # elevation floor the LAST request carried, the higher of asind(min_height/L) and min_elevation + candidate_elevation_margin at the length it was made for; the optimizer constrains HEIGHT, and only at the far end of the lap's reel-out [deg]
    elevation_min_extra_deg: 0.0    # raise a rejected reply's shortfall added to that floor, carried forward once measured; 0 means no reply was gated out low [deg]
  inflow:
    wind_speed_m_s: 8.0             # wind speed at 6 m sent to the optimizer [m/s]
    wind_direction_deg: 270.0       # direction the wind comes from [deg]
    profile_law: 3                  # 0=CONST, 1=EXP, 2=LOG, 3=EXPLOG, 4-6=CUSTOM_*
  winch:
    k_v: 0.0408                     # v_set = k_v * sqrt(force) sent to the optimizer [-]
    f_min_N: 350.0                  # minimum winch force sent [N]
    f_max_N: 8000.0                 # maximum winch force sent [N]
performance:
  sim_time_s: 112.5                 # simulated time [s]
  wall_time_s: 106.6                # wall-clock time [s]
  total_wall_time_s: 125.4          # the whole script: package loading, init, settling, the startup solve and the run, up to this summary — the archive copy and any plots follow it [s]
  optimization_time_s: 52.9         # wall time waiting for the optimizer: the startup solve (8.6 s) plus every blocking re-optimization (traj_opt.reopt.blocked_s) [s]
  realtime_factor: 1.8              # sim_time / wall_time, excluding time frozen for re-optimization
  ms_per_step: 6.2                  # wall time per step, excluding time frozen for re-optimization [ms]
  steps: 10125                      # step count
  dt_s: 0.011111111111111112        # simulation timestep [s]
  vsm_interval: 10                  # VSM aerodynamic update interval [steps]
summary:
  date: "2026-08-22"                # wall-clock date the run finished
  time: "18:02:51"                  # wall-clock time the run finished
  wind_speed_m_s: 8.0               # wind speed at 6 m sent to the optimizer [m/s]
  mean_power_W: 21228               # mean reel-out power the run harvested [W]
  power_ratio: 0.98                 # measured / predicted mean reel-out power, against the weighted prediction
  total_wall_time_s: 125.4          # the whole script, start to this summary [s]
  realtime_factor: 1.8              # sim_time / wall_time, excluding time frozen for re-optimization
  optimization_requests: 6          # solves that completed, accepted or rejected
  optimizations_installed: 6        # new paths actually flown
  av_power_W: 21573                 # mean reel-out power over phase four [W]
  min_power_W: 15718                # min reel-out power over phase four [W]
  max_power_W: 23967                # max reel-out power over phase four [W]
  min_force_N: 5390                 # min tether force over phase four [N]
  av_force_N: 6535                  # mean tether force over phase four [N]
  max_force_N: 6849                 # max tether force over phase four [N]
  v_ro_min_m_s: 2.83                # min reel-out speed over phase four [m/s]
  v_ro_av_m_s: 3.3                  # mean reel-out speed over phase four [m/s]
  v_ro_max_m_s: 3.56                # max reel-out speed over phase four [m/s]
  av_depower: 0.27                  # mean KCU depower over phase four [-]
