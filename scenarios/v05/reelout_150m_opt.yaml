# Run summary for output/reelout_150m_opt.arrow, written by examples/simple_reelout.jl at the end of the run.
success_criteria: "all 9 passed"    # pass/fail verdict vs V3Kite's success criteria
simulation:
  script: "simple_opt_reelout.jl"   # script that produced this run
  project: "system_reelout_150m.yaml" # system project flown
  rel_turbulence: 0.0               # turbulence level in [0, 1] passed to init
  wind_speed: 5.0                   # mean wind speed in m/s passed to init
  time: "23:41:12"                  # wall-clock time the run finished
  date: "2026-08-26"                # wall-clock date the run finished
  hostname: "framework"             # machine the run executed on
  git_hash: "636c45a"               # SimpleKiteControllers.jl commit hash
  git_status: "clean"               # SimpleKiteControllers.jl working tree: clean or dirty
fig8_metrics:
  settled_from_s: 32.6              # sim time the scoring window begins [s]
  settle_time_s: 30.6               # time after t_start to converge [s]
  laps: 8.0                         # figure-eight laps completed
  cross_track_deg:
    rms: 1.58                       # RMS cross-track error [deg]
    mean: 1.22                      # mean cross-track error [deg]
    max: 4.94                       # max cross-track error [deg]
  elevation_deg:
    min_settled: 9.4                # min elevation, settled window [deg]
    min_whole_run: 9.4              # min elevation, whole run [deg]
  peak_turn_rate_deg_s: 89          # peak heading rate [deg/s]
  extent:
    azimuth_deg:
      min: -18.2                    # flown azimuth reach, negative side [deg]
      max: 16.7                     # flown azimuth reach, positive side [deg]
    azimuth_pct_of_amplitude:
      min: 96                       # negative reach as % of the pattern's ±A
      max: 95                       # positive reach as % of the pattern's ±A
    worst_lobe_deg:
      min: -13.2                    # weakest lobe, negative side [deg]
      max: 13.2                     # weakest lobe, positive side [deg]
    elevation_span_deg: 20.2        # flown elevation span over the whole settled window — the pattern DESCENDS as the tether grows, so this is mostly that descent [deg]
    elevation_span_lap_deg: 7.5     # flown elevation span per lobe, which is the pattern's own height [deg]
    elevation_span_pct_of_b: 78     # flown span as % of the B commanded where it was flown
  tether_force_N:
    mean: 1940                      # mean tether force, settled window [N]
    std: 357                        # std tether force, settled window [N]
    cv_pct: 18.4                    # coefficient of variation [%]
  steering:
    peak_abs_u_s: 0.32              # peak |rel_steering| commanded
    pct_time_within_2pct_of_peak: 11 # % of time within 2% of peak (saturation)
    hf_std_steering: 0.0143         # high-frequency std of steering (chatter)
    hf_std_turnrate_deg_s: 0.41     # high-frequency std of heading rate [deg/s]
  tape:
    delivered_peak_abs_u_s: 0.318   # peak steering the KCU tape delivered
    commanded_peak_abs_u_s: 0.32    # peak steering commanded
    rate_limited_pct_time: 11       # % of time the tape's rate limit was hit
    rate_limited_peak_per_s: 0.2    # peak tape rate reached [1/s]
    rate_limit_per_s: 0.2           # KCU's configured rate limit [1/s]
reelout:
  v_app_phase4:
    duration_s: 135.6               # phase-4 window length [s]
    mean_m_s: 16.78                 # mean apparent wind speed [m/s]
    min_m_s: 14.51                  # min apparent wind speed [m/s]
    max_m_s: 19.85                  # max apparent wind speed [m/s]
    v_app_ref_m_s: 27.0             # reference apparent wind speed [m/s]
    deviation_pct: -37.8            # mean v_app deviation from v_app_ref [%]
  tether:
    start_m: 150.0                  # tether length at run start [m]
    end_m: 380.0                    # tether length at run end [m]
    target_m: 380.0                 # reelout_l_max target [m]
    stop_reason: "length"           # criterion that ended reel-out: length, laps, or none
    laps_reeled: 8.01               # figure-eight laps completed by the time reel-out ended
  force:
    cf_force_ro: 1.15               # crest factor: peak / mean tether force over the reeling window
  power:
    cf_power_ro: 1.26               # crest factor: peak / mean reel-out power over the reeling window
    duration_s: 142.3               # reeling window length [s]
    n_samples: 12808                # sample count in the reeling window
    energy_kJ: 540.1                # energy over the reeling window [kJ]
    energy_run_kJ: 530.3            # energy over the whole run [kJ]
  winch_state:
    speed_pct: 100.0                # % of the reeling window in speed control (state 1)
    lower_force_pct: 0.0            # % in the LowerForceController, reeling in (state 0)
    upper_force_pct: 0.0            # % in the UpperForceController, force capped at f_high (state 2)
    n_samples: 12808                # sample count in the reeling window
  ringing:
    n_peaks: 5                      # ring peaks detected above peak_floor
    period_s: 3.13                  # mean peak-to-peak ring period [s]
    zeta: 0.027                     # damping ratio from the peak log decrement
    overshoot_m_s: 0.05             # first ring peak's amplitude above the local trend [m/s]
    duration_s: 20.0                # time until the ring decays below settle_frac of overshoot_m_s [s]
    peak_v_reelout_m_s: 2.06        # raw v_reelout max within ring_span [m/s]
    steady_v_reelout_m_s: 1.85      # mean v_reelout after ring_span [m/s]
traj_opt:
  power:
    predicted_W: 3764               # predicted mean reel-out power of the paths actually flown, weighted by their share of the reeling window [W]
    predicted_initial_W: 3334       # predicted mean reel-out power of the path installed before the run [W]
    predicted_paths:
      t_000.0_s: 3334               # 17% of the reeling window
      t_050.4_s: 3480               # 14% of the reeling window
      t_070.2_s: 3809               # 13% of the reeling window
      t_089.3_s: 3950               # 14% of the reeling window
      t_108.6_s: 3984               # 14% of the reeling window
      t_128.8_s: 3964               # 16% of the reeling window
      t_151.0_s: 3919               # 12% of the reeling window
    measured_W: 3795                # mean reel-out power the run harvested [W]
    ratio: 1.01                     # measured / predicted, against the weighted prediction
  guess:
    a_deg: 30.0                     # width of the guess lemniscate; azimuth spans ±a [deg]
    b_deg: 12.0                     # height of the guess, peak to peak [deg]
    el_center_deg: 29.0             # centre elevation of the guess [deg]
    points: 361                     # points the guess was sent with
    depower_seed_m: 1.6             # power-tape length the solve started from, input_depower ramped with the wind above input_depower_wind_ref; only a seed, the server optimizes it [m]
    depower_optimized:
      t_000.0_s: 1.484              # optimizer's l_dp [m]; rel_depower equivalent 0.276 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_050.4_s: 1.48               # optimizer's l_dp [m]; rel_depower equivalent 0.275 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_070.2_s: 1.466              # optimizer's l_dp [m]; rel_depower equivalent 0.272 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_089.3_s: 1.456              # optimizer's l_dp [m]; rel_depower equivalent 0.270 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_108.6_s: 1.449              # optimizer's l_dp [m]; rel_depower equivalent 0.269 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_128.8_s: 1.447              # optimizer's l_dp [m]; rel_depower equivalent 0.268 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_151.0_s: 1.446              # optimizer's l_dp [m]; rel_depower equivalent 0.268 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
    k_v_optimized:
  path:
    points: 360                     # points of the path installed before the run
    points_final: 360               # points of the path at the end; differs when reopt installed one
    az_center_deg: 1.6              # centre azimuth of the path [deg]
    az_amplitude_deg: 23.5          # half-width of the STARTUP path; the criteria are scored against the pattern commanded at each lap, whose mean is lift_budget's reference [deg]
    el_center_deg: 24.3             # centre elevation of the path [deg]
    el_height_deg: 13.6             # elevation span of the path, peak to peak [deg]
    downloops: true                 # traversal direction the optimizer solved for
  feasibility:
    min_required: 0.82              # min_feasibility_margin of data/traj_opt.yaml [-]
    turn_radius_headroom: 1.2       # factor on the gate's number for what the GATE adds: its finite-difference curvature estimate and the elevation lift [-]
    turn_radius_request_m: 12.78    # minimum turn radius asked of the optimizer for the LAST request; the gate's margin/(c1*max_steering) times the headroom and the lap's reel-out ratio, since the optimizer measures the radius up the reel-out while the run flies the curve at the anchor [m]
    turn_radius_scale: 1.362        # the two corrections together, as sent [-]
    turn_radius_lap_reelout_m: 0.0  # reel-out per lap ASSUMED for the startup request, the only one with no reply to measure it off [m]
    margin_start: 0.88              # curvature margin at the starting length; the worst case only when one path is flown throughout — see the docstring [-]
    margin_end: 2.22                # curvature margin at reelout_l_max [-]
    c1_final: 0.1919                # turn-rate gain at depower_final [1/m]
    margin_final: 1.52              # curvature margin phase 5 flies with: depower_final's c1, at reelout_l_max, on the STARTING path lifted by el_offset_final [-]
    margin_final_flown: 0.67        # the same, for the last path the re-optimizer installed — the one phase 5 actually inherited [-]
  reopt:
    enabled: true                   # re-optimization during the run
    warm_start: true                # /step alone, seeded from the previous optimum; false re-inits from the parametric guess at every length
    requests: 6                     # solves that completed, accepted or rejected
    blocking: true                  # simulation held while a solve ran
    blocked_s: 33.3                 # wall time the simulation was frozen waiting for replies [s]
    installed: 6                    # new paths actually flown
    blend_retries: 0                # cold-restart attempts spent on a rejected reply — a folded blend, a collapsed prediction, or a clearance/elevation shortfall re-asked at a raised floor — across every install this run
    events:
      t_050.4_s: "installed — margin 1.01 (phase 5: 1.65 at 380 m), clearance 45.8 m, 3480 W predicted" # at L = 161 m
      t_070.2_s: "installed — margin 1.00 (phase 5: 1.34 at 380 m), clearance 49.0 m, 3809 W predicted" # at L = 198 m
      t_089.3_s: "installed — margin 1.00 (phase 5: 1.13 at 380 m), clearance 50.6 m, 3950 W predicted" # at L = 234 m
      t_108.6_s: "installed — margin 1.03 (phase 5: 1.00 at 380 m), clearance 51.7 m, 3984 W predicted" # at L = 271 m
      t_128.8_s: "installed — margin 0.96 (phase 5: 0.82 at 380 m), clearance 58.4 m, 3964 W predicted" # at L = 309 m
      t_151.0_s: "installed — margin 0.90 (phase 5: 0.67 at 380 m), clearance 64.6 m, 3919 W predicted" # at L = 351 m
  el_bias:
    gain: 0.5                       # per-lap learning gain for the elevation bias; 0 = off
    final_deg: 1.99                 # mean elevation correction added to the last installed path [deg]
    bins: 5                         # el_bias_bins, azimuth bands the correction is learnt over; 1 = one rigid shift
    smooth: 0.25                    # el_bias_smooth, how far each band is pulled towards its neighbours after every lap [-]
    profile_deg: [0.92, 1.32, 2.19, 3.12, 2.39] # the learnt correction per band, crossing first [deg]
    lift_deg: 1.5                   # el_offset_final, the fixed lift added on top of the learnt bias [deg]
    lift_lead_s: 8.0                # el_offset_lead, how early the lift is allowed to latch; 0 = at the end [s]
    wing_deg: 1.5                   # el_offset_wing, extra lift at the lobes, baked into every installed path [deg]
    wing_mode: "azimuth"            # el_offset_wing_mode, the coordinate that lift is shaped over
    wing_az_deg: 10.0               # azimuth mode: azimuth beyond which that lift is full; it ramps over el_offset_wing_blend below it [deg]
    wing_depth: 0.0                 # elevation mode: depth below the path's elevation centre where the lift starts, in half-spans; full at its lowest point [-]
    lift_t_s: 159.3                 # when the lift actually latched [s]
    lift_remaining_m: 14.3          # reel-out left at that moment; > 0 means the lead fired, 0 means phase 5 did [m]
    shift_delivery:
      t_054.4_s: "blended in"       # +0.42° of shift (mean over the bands), curvature margin 1.07 vs 0.82 required
      t_074.2_s: "blended in"       # +0.20° of shift (mean over the bands), curvature margin 1.07 vs 0.82 required
      t_093.3_s: "blended in"       # -0.04° of shift (mean over the bands), curvature margin 1.01 vs 0.82 required
      t_112.7_s: "blended in"       # +0.01° of shift (mean over the bands), curvature margin 0.97 vs 0.82 required
      t_132.8_s: "blended in"       # +0.06° of shift (mean over the bands), curvature margin 0.93 vs 0.82 required
      t_155.0_s: "blended in"       # +0.12° of shift (mean over the bands), curvature margin 0.91 vs 0.82 required
      t_159.3_s: "blended in"       # +1.50° of shift (mean over the bands), curvature margin 0.91 vs 0.82 required
      t_202.7_s: "held back"        # +1.21° of shift (mean over the bands), curvature margin 0.54 vs 0.82 required
    laps:
      lap_1: -0.82                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.82°, correction became [+0.14 +0.25 +0.49 +0.67 +0.55]° (crossing … lobe)
      lap_2: -0.36                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.77°, correction became [+0.08 +0.29 +0.77 +1.14 +0.83]° (crossing … lobe)
      lap_3: 0.1                    # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.50°, correction became [-0.12 +0.14 +0.75 +1.24 +0.92]° (crossing … lobe)
      lap_4: 0.02                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.53°, correction became [-0.23 +0.05 +0.79 +1.39 +0.97]° (crossing … lobe)
      lap_5: -0.06                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.59°, correction became [-0.28 +0.01 +0.88 +1.58 +1.10]° (crossing … lobe)
      lap_6: -0.16                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.71°, correction became [-0.24 +0.06 +1.01 +1.77 +1.28]° (crossing … lobe)
      lap_8: -1.12                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.68°, correction became [+0.92 +1.32 +2.19 +3.12 +2.39]° (crossing … lobe)
    lap_profiles:
      lap_1: "-0.22 -0.50 -1.00 -1.49 -1.00" # error vs the optimizer's curve per band, crossing first; 318/323/307/255/385 samples, correction now [+0.14 +0.25 +0.49 +0.67 +0.55]° (crossing … lobe)
      lap_2: "+0.19 -0.04 -0.60 -1.25 -0.29" # error vs the optimizer's curve per band, crossing first; 359/378/362/290/390 samples, correction now [+0.08 +0.29 +0.77 +1.14 +0.83]° (crossing … lobe)
      lap_3: "+0.50 +0.36 -0.02 -0.58 +0.10" # error vs the optimizer's curve per band, crossing first; 340/381/364/281/359 samples, correction now [-0.12 +0.14 +0.75 +1.24 +0.92]° (crossing … lobe)
      lap_4: "+0.30 +0.25 -0.15 -0.75 +0.25" # error vs the optimizer's curve per band, crossing first; 332/397/398/262/350 samples, correction now [-0.23 +0.05 +0.79 +1.39 +0.97]° (crossing … lobe)
      lap_5: "+0.18 +0.18 -0.24 -0.91 +0.14" # error vs the optimizer's curve per band, crossing first; 359/428/440/220/364 samples, correction now [-0.28 +0.01 +0.88 +1.58 +1.10]° (crossing … lobe)
      lap_6: "-0.02 +0.03 -0.33 -0.93 +0.07" # error vs the optimizer's curve per band, crossing first; 409/502/508/200/385 samples, correction now [-0.24 +0.06 +1.01 +1.77 +1.28]° (crossing … lobe)
      lap_8: "-1.09 -1.26 -1.18 -1.75 -0.80" # error vs the optimizer's curve per band, crossing first; 546/724/548/84/537 samples, correction now [+0.92 +1.32 +2.19 +3.12 +2.39]° (crossing … lobe)
  droop_profile:
    az_00_20_pct: 2.98              # kite below the path's elevation centre [deg], over 3073 samples; the path itself sits 0.68 half-spans down there and the kite 0.01° under it
    az_20_40_pct: 2.27              # kite below the path's elevation centre [deg], over 3690 samples; the path itself sits 0.45 half-spans down there and the kite -0.38° under it
    az_40_60_pct: 1.46              # kite below the path's elevation centre [deg], over 3524 samples; the path itself sits 0.03 half-spans down there and the kite -1.27° under it
    az_60_80_pct: 2.25              # kite below the path's elevation centre [deg], over 1761 samples; the path itself sits -0.00 half-spans down there and the kite -2.10° under it
    az_80_100_pct: 1.87             # kite below the path's elevation centre [deg], over 3277 samples; the path itself sits 0.12 half-spans down there and the kite -1.28° under it
    centre_to_lobe_deg: 0.0         # how much deeper the kite flies in its worst azimuth band than in the crossing — what a SHAPED lift is aimed at, where el_bias and el_offset_final can only move the pattern rigidly [deg]
  lift_budget:
    delivered_deg: 2.28             # mean correction the path in the air actually carries — el_bias plus el_offset_final once it has been delivered [deg]
    delivered_profile_deg: [1.26, 1.56, 2.51, 3.27, 2.78] # the same, per azimuth band, crossing first [deg]
    commanded_az_amp_deg: 18.26     # mean half-width the run was COMMANDED to fly, against the startup path's in traj_opt.path [deg]
    commanded_el_height_deg: 9.92   # mean commanded pattern height [deg]
    wing_deg: 1.5                   # el_offset_wing, the fixed lobe lift baked into every installed path [deg]
    el_min_run_deg: 9.4             # lowest elevation over the whole run — set in phase 4, so a phase-5 lift does not move it [deg]
    el_min_final_deg: 9.4           # lowest elevation in phase 5, which is what el_offset_final buys [deg]
    azimuth_reach_pos_deg: 4.6      # reach margin: flew 17.38° against the 12.78° required by min_span_frac = 0.70, +36 %
    azimuth_reach_neg_deg: 4.78     # reach margin: flew 17.56° against the 12.78° required by min_span_frac = 0.70, +37 %
    elevation_span_deg: 0.77        # reach margin: flew 7.71° against the 6.94° required by min_span_frac = 0.70, +11 %
    tightest: "elevation span"      # the size criterion with the least room, +0.77° (+11 %) — what the NEXT lift has to spend
  clearance:
    path_min_m: 45.2                # lowest point of the PRE-FLIGHT path at the starting length [m]
    flown_min_m: 34.7               # lowest point the kite actually reached over the run [m]
    min_required_m: 40.0            # min_height of data/traj_opt.yaml [m]
    elevation_min_asked_deg: 9.5    # elevation floor the LAST request carried, the higher of asind(min_height/L) and min_elevation + candidate_elevation_margin at the length it was made for; the optimizer constrains HEIGHT, and only at the far end of the lap's reel-out [deg]
    elevation_min_extra_deg: 0.0    # raise a rejected reply's shortfall added to that floor, carried forward once measured; 0 means no reply was gated out low [deg]
  inflow:
    wind_speed_m_s: 5.0             # wind speed at 6 m sent to the optimizer [m/s]
    wind_direction_deg: 270.0       # direction the wind comes from [deg]
    profile_law: 3                  # 0=CONST, 1=EXP, 2=LOG, 3=EXPLOG, 4-6=CUSTOM_*
  winch:
    k_v: 0.0408                     # v_set = k_v * sqrt(force) sent to the optimizer [-]
    optimize_k_v: false             # k_v sent as a design variable; the reply's value is then the one flown
    k_v_flown: 0.0408               # k_v the run actually flew; == k_v, the optimizer did not move it
    f_min_N: 700.0                  # minimum winch force sent [N]
    f_max_N: 8000.0                 # maximum winch force sent [N]
    force_limit: "hard"             # bare kv*sqrt(force) with the two force controllers switching in at the limits
performance:
  sim_time_s: 203.0                 # simulated time [s]
  wall_time_s: 104.6                # wall-clock time [s]
  total_wall_time_s: 236.2          # the whole script: package loading, init, settling, the startup solve and the run, up to this summary — the archive copy and any plots follow it [s]
  optimization_time_s: 46.6         # wall time waiting for the optimizer: the startup solve (13.3 s) plus every blocking re-optimization (traj_opt.reopt.blocked_s) [s]
  realtime_factor: 2.85             # sim_time / wall_time, excluding time frozen for re-optimization
  ms_per_step: 3.9                  # wall time per step, excluding time frozen for re-optimization [ms]
  steps: 18272                      # step count
  dt_s: 0.011111111111111112        # simulation timestep [s]
  vsm_interval: 10                  # VSM aerodynamic update interval [steps]
summary:
  date: "2026-08-26"                # wall-clock date the run finished
  time: "23:41:12"                  # wall-clock time the run finished
  wind_speed_gnd: 5.0               # wind speed at 6 m sent to the optimizer [m/s]
  power_ratio: 1.01                 # measured / predicted mean reel-out power, against the weighted prediction
  total_wall_time_s: 236.2          # the whole script, start to this summary [s]
  realtime_factor: 2.85             # sim_time / wall_time, excluding time frozen for re-optimization
  optimization_requests: 6          # solves that completed, accepted or rejected
  optimizations_installed: 6        # new paths actually flown
  av_power_ro: 3910                 # mean reel-out power over phase four [W]
  min_power_ro: 71                  # min reel-out power over phase four [W]
  max_power_ro: 4784                # max reel-out power over phase four [W]
  min_force_ro: 1693                # min tether force over phase four [N]
  av_force_ro: 2100                 # mean tether force over phase four [N]
  max_force_ro: 2379                # max tether force over phase four [N]
  v_ro_min: 0.04                    # min reel-out speed over phase four [m/s]
  v_ro_av: 1.86                     # mean reel-out speed over phase four [m/s]
  v_ro_max: 2.06                    # max reel-out speed over phase four [m/s]
  av_depower_ro: 0.272              # mean KCU depower over phase four [-]
