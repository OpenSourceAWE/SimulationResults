# Run summary for output/reelout_150m_opt.arrow, written by examples/simple_reelout.jl at the end of the run.
success_criteria: "all 10 passed"   # pass/fail verdict vs V3Kite's success criteria
simulation:
  script: "simple_opt_reelout.jl"   # script that produced this run
  project: "system_reelout_150m.yaml" # system project flown
  rel_turbulence: 0.0               # turbulence level in [0, 1] passed to init
  wind_speed: 4.0                   # mean wind speed in m/s passed to init
  time: "11:38:49"                  # wall-clock time the run finished
  date: "2026-08-30"                # wall-clock date the run finished
  hostname: "ufryzen"               # machine the run executed on
  git_hash: "68b1d1e"               # SimpleKiteControllers.jl commit hash
  git_status: "dirty"               # SimpleKiteControllers.jl working tree: clean or dirty
fig8_metrics:
  settled_from_s: 27.6              # sim time the scoring window begins [s]
  settle_time_s: 25.6               # time after t_start to converge [s]
  laps: 8.0                         # figure-eight laps completed
  cross_track_deg:
    rms: 2.6                        # RMS cross-track error [deg]
    mean: 2.19                      # mean cross-track error [deg]
    max: 7.14                       # max cross-track error [deg]
  elevation_deg:
    min_settled: 9.4                # min elevation, settled window [deg]
    min_whole_run: 9.4              # min elevation, whole run [deg]
  peak_turn_rate_deg_s: 64          # peak heading rate [deg/s]
  extent:
    azimuth_deg:
      min: -20.8                    # flown azimuth reach, negative side [deg]
      max: 20.2                     # flown azimuth reach, positive side [deg]
    azimuth_pct_of_amplitude:
      min: 95                       # negative reach as % of the pattern's ±A
      max: 97                       # positive reach as % of the pattern's ±A
    worst_lobe_deg:
      min: -17.2                    # weakest lobe, negative side [deg]
      max: 16.2                     # weakest lobe, positive side [deg]
    elevation_span_deg: 22.6        # flown elevation span over the whole settled window — the pattern DESCENDS as the tether grows, so this is mostly that descent [deg]
    elevation_span_lap_deg: 9.4     # flown elevation span per lobe, which is the pattern's own height [deg]
    elevation_span_pct_of_b: 88     # flown span as % of the B commanded where it was flown
  tether_force_N:
    mean: 1089                      # mean tether force, settled window [N]
    std: 252                        # std tether force, settled window [N]
    cv_pct: 23.2                    # coefficient of variation [%]
  steering:
    peak_abs_u_s: 0.32              # peak |rel_steering| commanded
    pct_time_within_2pct_of_peak: 13 # % of time within 2% of peak (saturation)
    hf_std_steering: 0.0114         # high-frequency std of steering (chatter)
    hf_std_turnrate_deg_s: 0.17     # high-frequency std of heading rate [deg/s]
  tape:
    delivered_peak_abs_u_s: 0.32    # peak steering the KCU tape delivered
    commanded_peak_abs_u_s: 0.32    # peak steering commanded
    rate_limited_pct_time: 7        # % of time the tape's rate limit was hit
    rate_limited_peak_per_s: 0.2    # peak tape rate reached [1/s]
    rate_limit_per_s: 0.2           # KCU's configured rate limit [1/s]
reelout:
  v_app_phase4:
    duration_s: 195.3               # phase-4 window length [s]
    mean_m_s: 12.71                 # mean apparent wind speed [m/s]
    min_m_s: 9.57                   # min apparent wind speed [m/s]
    max_m_s: 18.6                   # max apparent wind speed [m/s]
    v_app_ref_m_s: 27.0             # reference apparent wind speed [m/s]
    deviation_pct: -52.9            # mean v_app deviation from v_app_ref [%]
  tether:
    start_m: 150.0                  # tether length at run start [m]
    end_m: 380.0                    # tether length at run end [m]
    target_m: 380.0                 # reelout_l_max target [m]
    stop_reason: "length"           # criterion that ended reel-out: length, laps, or none
    laps_reeled: 7.93               # figure-eight laps completed by the time reel-out ended
  force:
    cf_force_ro: 1.47               # crest factor: peak / mean tether force over the reeling window
  power:
    cf_power_ro: 1.7                # crest factor: peak / mean reel-out power over the reeling window
    duration_s: 203.6               # reeling window length [s]
    n_samples: 18321                # sample count in the reeling window
    energy_kJ: 316.3                # energy over the reeling window [kJ]
    energy_run_kJ: 305.0            # energy over the whole run [kJ]
  winch_state:
    speed_pct: 100.0                # % of the reeling window in speed control (state 1)
    lower_force_pct: 0.0            # % in the LowerForceController, reeling in (state 0)
    upper_force_pct: 0.0            # % in the UpperForceController, force capped at f_high (state 2)
    n_samples: 18321                # sample count in the reeling window
  ringing:
    n_peaks: 5                      # ring peaks detected above peak_floor
    period_s: 2.58                  # mean peak-to-peak ring period [s]
    zeta: 0.056                     # damping ratio from the peak log decrement
    overshoot_m_s: 0.1              # first ring peak's amplitude above the local trend [m/s]
    duration_s: 20.0                # time until the ring decays below settle_frac of overshoot_m_s [s]
    peak_v_reelout_m_s: 1.5         # raw v_reelout max within ring_span [m/s]
    steady_v_reelout_m_s: 1.32      # mean v_reelout after ring_span [m/s]
traj_opt:
  power:
    predicted_W: 1505               # predicted mean reel-out power of the paths actually flown, weighted by their share of the reeling window [W]
    predicted_initial_W: 1235       # predicted mean reel-out power of the path installed before the run [W]
    predicted_paths:
      t_000.0_s: 1235               # 17% of the reeling window
      t_054.9_s: 1300               # 15% of the reeling window
      t_085.8_s: 1515               # 15% of the reeling window
      t_116.9_s: 1629               # 16% of the reeling window
      t_149.7_s: 1667               # 17% of the reeling window
      t_184.6_s: 1654               # 19% of the reeling window
    measured_W: 1554                # mean reel-out power the run harvested [W]
    ratio: 1.03                     # measured / predicted, against the weighted prediction
    free_speed_reference_W: 1547    # mean reel-out power an OPTIMAL path could harvest with ANY winch in [f_min, f_max], solved after the run at 6 lengths and weighted by time spent at each. An UPPER BOUND, not a prediction of this k_v law, and never flown [W]
    free_speed_points:
      L_119.1_m: 1052               # free_speed solve [W]
      L_171.3_m: 1573               # free_speed solve [W]
      L_223.4_m: 1656               # free_speed solve [W]
      L_275.6_m: 1649               # free_speed solve [W]
      L_327.8_m: 1630               # free_speed solve [W]
      L_380.0_m: 1540               # free_speed solve [W]
    free_speed_ratio: 1.0           # measured / free_speed_reference_W; above 1 means the run harvests more than the optimizer's own upper bound, which no winch law can explain
  guess:
    a_deg: 30.0                     # width of the guess lemniscate; azimuth spans ±a [deg]
    b_deg: 12.0                     # height of the guess, peak to peak [deg]
    el_center_deg: 29.0             # centre elevation of the guess [deg]
    points: 361                     # points the guess was sent with
    depower_seed_m: 1.6             # power-tape length the solve started from, input_depower ramped with the wind above input_depower_wind_ref; only a seed, the server optimizes it [m]
    depower_optimized:
      t_000.0_s: 1.529              # optimizer's l_dp [m]; rel_depower equivalent 0.287 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_054.9_s: 1.525              # optimizer's l_dp [m]; rel_depower equivalent 0.286 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_085.8_s: 1.511              # optimizer's l_dp [m]; rel_depower equivalent 0.283 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_116.9_s: 1.496              # optimizer's l_dp [m]; rel_depower equivalent 0.280 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_149.7_s: 1.485              # optimizer's l_dp [m]; rel_depower equivalent 0.278 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_184.6_s: 1.482              # optimizer's l_dp [m]; rel_depower equivalent 0.277 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
    k_v_optimized:
  path:
    points: 360                     # points of the path installed before the run
    points_final: 360               # points of the path at the end; differs when reopt installed one
    az_center_deg: 1.7              # centre azimuth of the path [deg]
    az_amplitude_deg: 26.2          # half-width of the STARTUP path; the criteria are scored against the pattern commanded at each lap, whose mean is lift_budget's reference [deg]
    el_center_deg: 26.5             # centre elevation of the path [deg]
    el_height_deg: 16.3             # elevation span of the path, peak to peak [deg]
    downloops: true                 # traversal direction the optimizer solved for
  feasibility:
    min_required: 0.82              # min_feasibility_margin of data/traj_opt.yaml [-]
    turn_radius_headroom: 1.25      # factor on the gate's number for what the GATE adds: its finite-difference curvature estimate and the elevation lift [-]
    turn_radius_request_m: 15.05    # minimum turn radius asked of the optimizer for the LAST request; the gate's margin/(c1*max_steering) times the headroom and the lap's reel-out ratio, since the optimizer measures the radius up the reel-out while the run flies the curve at the anchor [m]
    turn_radius_scale: 1.413        # the two corrections together, as sent [-]
    turn_radius_lap_reelout_m: 0.0  # reel-out per lap ASSUMED for the startup request, the only one with no reply to measure it off [m]
    margin_start: 0.97              # curvature margin at the starting length; the worst case only when one path is flown throughout — see the docstring [-]
    margin_end: 2.45                # curvature margin at reelout_l_max [-]
    c1_final: 0.1655                # turn-rate gain at depower_final [1/m]
    margin_final: 1.63              # curvature margin phase 5 flies with: depower_final's c1, at reelout_l_max, on the STARTING path lifted by el_offset_final [-]
    margin_final_flown: 0.82        # the same, for the last path the re-optimizer installed — the one phase 5 actually inherited [-]
  reopt:
    enabled: true                   # re-optimization during the run
    warm_start: true                # /step alone, seeded from the previous optimum; false re-inits from the parametric guess at every length
    requests: 5                     # solves that completed, accepted or rejected
    blocking: true                  # simulation held while a solve ran
    blocked_s: 22.1                 # wall time the simulation was frozen waiting for replies [s]
    installed: 5                    # new paths actually flown
    blend_retries: 0                # cold-restart attempts spent on a rejected reply — a folded blend, a collapsed prediction, or a clearance/elevation shortfall re-asked at a raised floor — across every install this run
    events:
      t_054.9_s: "installed — margin 0.99 (phase 5: 1.62 at 380 m), clearance 47.6 m, 1300 W predicted" # at L = 158 m
      t_085.8_s: "installed — margin 0.97 (phase 5: 1.28 at 380 m), clearance 52.5 m, 1515 W predicted" # at L = 196 m
      t_116.9_s: "installed — margin 0.96 (phase 5: 1.05 at 380 m), clearance 56.3 m, 1629 W predicted" # at L = 237 m
      t_149.7_s: "installed — margin 0.98 (phase 5: 0.90 at 380 m), clearance 59.6 m, 1667 W predicted" # at L = 283 m
      t_184.6_s: "installed — margin 1.05 (phase 5: 0.82 at 380 m), clearance 68.5 m, 1654 W predicted" # at L = 331 m
  el_bias:
    gain: 0.5                       # per-lap learning gain for the elevation bias; 0 = off
    final_deg: 3.21                 # mean elevation correction added to the last installed path [deg]
    bins: 5                         # el_bias_bins, azimuth bands the correction is learnt over; 1 = one rigid shift
    smooth: 0.25                    # el_bias_smooth, how far each band is pulled towards its neighbours after every lap [-]
    profile_deg: [2.41, 2.65, 3.48, 4.0, 3.49] # the learnt correction per band, crossing first [deg]
    lift_deg: 3.0                   # el_offset_final, the fixed lift added on top of the learnt bias [deg]
    lift_lead_s: 8.0                # el_offset_lead, how early the lift is allowed to latch; 0 = at the end [s]
    wing_deg: 1.5                   # el_offset_wing, extra lift at the lobes, baked into every installed path [deg]
    wing_mode: "azimuth"            # el_offset_wing_mode, the coordinate that lift is shaped over
    wing_az_deg: 10.0               # azimuth mode: azimuth beyond which that lift is full; it ramps over el_offset_wing_blend below it [deg]
    wing_depth: 0.0                 # elevation mode: depth below the path's elevation centre where the lift starts, in half-spans; full at its lowest point [-]
    lift_t_s: 213.6                 # when the lift actually latched [s]
    lift_remaining_m: 9.4           # reel-out left at that moment; > 0 means the lead fired, 0 means phase 5 did [m]
    shift_delivery:
      t_058.9_s: "blended in (100 % of the mean, 50 % of the spread)" # +0.87° of shift (mean over the bands), curvature margin 0.90 vs 0.82 required
      t_062.9_s: "blended in (100 % of the mean, 50 % of the spread)" # -0.00° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_066.9_s: "blended in (100 % of the mean, 25 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_070.9_s: "blended in (100 % of the mean, 25 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.83 vs 0.82 required
      t_074.9_s: "blended in (100 % of the mean, 75 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_079.0_s: "blended in"       # +0.00° of shift (mean over the bands), curvature margin 0.83 vs 0.82 required
      t_089.9_s: "blended in (100 % of the mean, 75 % of the spread)" # +0.35° of shift (mean over the bands), curvature margin 0.83 vs 0.82 required
      t_093.9_s: "blended in"       # +0.00° of shift (mean over the bands), curvature margin 0.83 vs 0.82 required
      t_120.9_s: "blended in"       # +0.18° of shift (mean over the bands), curvature margin 1.02 vs 0.82 required
      t_153.7_s: "blended in"       # +0.08° of shift (mean over the bands), curvature margin 1.01 vs 0.82 required
      t_188.6_s: "blended in"       # -0.02° of shift (mean over the bands), curvature margin 0.96 vs 0.82 required
      t_213.6_s: "blended in"       # +3.00° of shift (mean over the bands), curvature margin 1.04 vs 0.82 required
      t_268.8_s: "held back"        # +1.75° of shift (mean over the bands), curvature margin 0.78 vs 0.82 required
    laps:
      lap_1: -1.63                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.63°, correction became [+0.57 +0.66 +0.94 +1.32 +0.86]° (crossing … lobe)
      lap_2: -0.61                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.44°, correction became [+0.75 +0.92 +1.39 +1.85 +1.19]° (crossing … lobe)
      lap_3: -1.02                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -2.93°, correction became [+0.70 +0.92 +1.52 +2.21 +1.63]° (crossing … lobe)
      lap_4: -0.94                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -3.05°, correction became [+0.69 +0.92 +1.61 +2.37 +1.81]° (crossing … lobe)
      lap_5: 0.07                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.31°, correction became [+0.66 +0.91 +1.68 +2.34 +1.71]° (crossing … lobe)
      lap_7: -1.71                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -2.99°, correction became [+2.41 +2.65 +3.48 +4.00 +3.49]° (crossing … lobe)
    lap_profiles:
      lap_1: "-1.15 -1.32 -1.85 -3.04 -1.34" # error vs the optimizer's curve per band, crossing first; 520/554/512/330/511 samples, correction now [+0.57 +0.66 +0.94 +1.32 +0.86]° (crossing … lobe)
      lap_2: "-0.33 -0.52 -0.91 -1.61 -0.14" # error vs the optimizer's curve per band, crossing first; 623/674/610/330/552 samples, correction now [+0.75 +0.92 +1.39 +1.85 +1.19]° (crossing … lobe)
      lap_3: "  -     -     -   -1.02   -  " # error vs the optimizer's curve per band, crossing first; 0/0/0/2/0 samples, correction now [+0.70 +0.92 +1.52 +2.21 +1.63]° (crossing … lobe)
      lap_4: "  -     -     -   -0.94   -  " # error vs the optimizer's curve per band, crossing first; 0/0/0/2/0 samples, correction now [+0.69 +0.92 +1.61 +2.37 +1.81]° (crossing … lobe)
      lap_5: "+0.10 +0.11 -0.18 -0.53 +0.73" # error vs the optimizer's curve per band, crossing first; 689/773/711/408/559 samples, correction now [+0.66 +0.91 +1.68 +2.34 +1.71]° (crossing … lobe)
      lap_7: "-1.74 -1.72 -1.78 -2.52 -1.40" # error vs the optimizer's curve per band, crossing first; 998/1134/729/260/910 samples, correction now [+2.41 +2.65 +3.48 +4.00 +3.49]° (crossing … lobe)
  droop_profile:
    az_00_20_pct: 4.88              # kite below the path's elevation centre [deg], over 5878 samples; the path itself sits 0.74 half-spans down there and the kite -1.39° under it
    az_20_40_pct: 3.74              # kite below the path's elevation centre [deg], over 6664 samples; the path itself sits 0.45 half-spans down there and the kite -1.69° under it
    az_40_60_pct: 2.51              # kite below the path's elevation centre [deg], over 5323 samples; the path itself sits 0.01 half-spans down there and the kite -2.39° under it
    az_60_80_pct: 3.64              # kite below the path's elevation centre [deg], over 2729 samples; the path itself sits 0.03 half-spans down there and the kite -3.20° under it
    az_80_100_pct: 2.49             # kite below the path's elevation centre [deg], over 4848 samples; the path itself sits 0.16 half-spans down there and the kite -1.85° under it
    centre_to_lobe_deg: 0.0         # how much deeper the kite flies in its worst azimuth band than in the crossing — what a SHAPED lift is aimed at, where el_bias and el_offset_final can only move the pattern rigidly [deg]
  lift_budget:
    delivered_deg: 4.46             # mean correction the path in the air actually carries — el_bias plus el_offset_final once it has been delivered [deg]
    delivered_profile_deg: [3.66, 3.91, 4.68, 5.34, 4.71] # the same, per azimuth band, crossing first [deg]
    commanded_az_amp_deg: 20.99     # mean half-width the run was COMMANDED to fly, against the startup path's in traj_opt.path [deg]
    commanded_el_height_deg: 10.57  # mean commanded pattern height [deg]
    wing_deg: 1.5                   # el_offset_wing, the fixed lobe lift baked into every installed path [deg]
    el_min_run_deg: 9.4             # lowest elevation over the whole run — set in phase 4, so a phase-5 lift does not move it [deg]
    el_min_final_deg: 9.37          # lowest elevation in phase 5, which is what el_offset_final buys [deg]
    azimuth_reach_pos_deg: 5.59     # reach margin: flew 20.28° against the 14.69° required by min_span_frac = 0.70, +38 %
    azimuth_reach_neg_deg: 5.19     # reach margin: flew 19.88° against the 14.69° required by min_span_frac = 0.70, +35 %
    elevation_span_deg: 1.94        # reach margin: flew 9.34° against the 7.40° required by min_span_frac = 0.70, +26 %
    tightest: "elevation span"      # the size criterion with the least room, +1.94° (+26 %) — what the NEXT lift has to spend
  clearance:
    path_min_m: 47.2                # lowest point of the PRE-FLIGHT path at the starting length [m]
    flown_min_m: 31.8               # lowest point the kite actually reached over the run [m]
    min_required_m: 40.0            # min_height of data/traj_opt.yaml [m]
    elevation_min_asked_deg: 9.5    # elevation floor the LAST request carried, the higher of asind(min_height/L) and min_elevation + candidate_elevation_margin at the length it was made for; the optimizer constrains HEIGHT, and only at the far end of the lap's reel-out [deg]
    elevation_min_extra_deg: 0.0    # raise a rejected reply's shortfall added to that floor, carried forward once measured; 0 means no reply was gated out low [deg]
  inflow:
    wind_speed_m_s: 4.0             # wind speed at 6 m sent to the optimizer [m/s]
    wind_direction_deg: 270.0       # direction the wind comes from [deg]
    profile_law: 3                  # 0=CONST, 1=EXP, 2=LOG, 3=EXPLOG, 4-6=CUSTOM_*
  winch:
    k_v: 0.0408                     # v_set = k_v * sqrt(force) sent to the optimizer [-]
    optimize_k_v: false             # k_v sent as a design variable; the reply's value is then the one flown
    k_v_flown: 0.0408               # k_v the run actually flew; == k_v, the optimizer did not move it
    f_min_N: 700.0                  # minimum winch force sent [N]
    f_max_N: 8000.0                 # maximum winch force sent [N]
    force_limit: "soft"             # the reel-out law itself limits the force, inverting the optimizer's own saturating tension curve (beta 3e-02/1e-03, force filtered at tau = 2.48 s); the UpperForceController is held in reset, so upper_force_pct is 0 by construction
performance:
  sim_time_s: 310.6                 # simulated time [s]
  wall_time_s: 98.4                 # wall-clock time [s]
  total_wall_time_s: 191.3          # the whole script: package loading, init, settling, the startup solve and the run, up to this summary — the archive copy and any plots follow it [s]
  optimization_time_s: 31.4         # wall time waiting for the optimizer: the startup solve (9.3 s) plus every blocking re-optimization (traj_opt.reopt.blocked_s) [s]
  realtime_factor: 4.07             # sim_time / wall_time, excluding time frozen for re-optimization
  ms_per_step: 2.7                  # wall time per step, excluding time frozen for re-optimization [ms]
  steps: 27952                      # step count
  dt_s: 0.011111111111111112        # simulation timestep [s]
  vsm_interval: 10                  # VSM aerodynamic update interval [steps]
summary:
  date: "2026-08-30"                # wall-clock date the run finished
  time: "11:38:49"                  # wall-clock time the run finished
  wind_speed_gnd: 4.0               # wind speed at 6 m sent to the optimizer [m/s]
  power_ratio: 1.03                 # measured / predicted reel-out power [-]
  power_ratio_free_speed: 1.0       # measured / free_speed reference reel-out power (1547 W, an UPPER BOUND over any winch in [f_min, f_max]); above 1 cannot be explained by any winch law [-]
  success_criteria: "all 10 passed" # pass/fail verdict vs V3Kite's success criteria
  total_wall_time_s: 191.3          # the whole script, start to this summary [s]
  realtime_factor: 4.07             # sim_time / wall_time, excluding time frozen for re-optimization
  optimization_requests: 5          # solves that completed, accepted or rejected
  optimizations_installed: 5        # new paths actually flown
  av_power_ro: 1590                 # mean reel-out power over phase four [W]
  min_power_ro: 44                  # min reel-out power over phase four [W]
  max_power_ro: 2645                # max reel-out power over phase four [W]
  min_force_ro: 901                 # min tether force over phase four [N]
  av_force_ro: 1206                 # mean tether force over phase four [N]
  max_force_ro: 1767                # max tether force over phase four [N]
  v_ro_min: 0.03                    # min reel-out speed over phase four [m/s]
  v_ro_av: 1.31                     # mean reel-out speed over phase four [m/s]
  v_ro_max: 1.57                    # max reel-out speed over phase four [m/s]
  av_depower_ro: 0.282              # mean KCU depower over phase four [-]
