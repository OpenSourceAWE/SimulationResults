# Run summary for output/reelout_150m_opt.arrow, written by examples/simple_reelout.jl at the end of the run.
success_criteria: "all 9 passed"    # pass/fail verdict vs V3Kite's success criteria
simulation:
  script: "simple_opt_reelout.jl"   # script that produced this run
  project: "system_reelout_150m.yaml" # system project flown
  rel_turbulence: "default"         # turbulence level in [0, 1] passed to init
  wind_speed: 4.0                   # mean wind speed in m/s passed to init
  time: "19:49:37"                  # wall-clock time the run finished
  date: "2026-08-25"                # wall-clock date the run finished
  hostname: "ufryzen"               # machine the run executed on
  git_hash: "f1e3300"               # SimpleKiteControllers.jl commit hash
  git_status: "dirty"               # SimpleKiteControllers.jl working tree: clean or dirty
fig8_metrics:
  settled_from_s: 36.3              # sim time the scoring window begins [s]
  settle_time_s: 34.3               # time after t_start to converge [s]
  laps: 9.5                         # figure-eight laps completed
  cross_track_deg:
    rms: 1.66                       # RMS cross-track error [deg]
    mean: 1.29                      # mean cross-track error [deg]
    max: 4.99                       # max cross-track error [deg]
  elevation_deg:
    min_settled: 9.8                # min elevation, settled window [deg]
    min_whole_run: 9.8              # min elevation, whole run [deg]
  peak_turn_rate_deg_s: 77          # peak heading rate [deg/s]
  extent:
    azimuth_deg:
      min: -19.6                    # flown azimuth reach, negative side [deg]
      max: 18.7                     # flown azimuth reach, positive side [deg]
    azimuth_pct_of_amplitude:
      min: 95                       # negative reach as % of the pattern's ±A
      max: 94                       # positive reach as % of the pattern's ±A
    worst_lobe_deg:
      min: -14.4                    # weakest lobe, negative side [deg]
      max: 14.2                     # weakest lobe, positive side [deg]
    elevation_span_deg: 23.3        # flown elevation span over the whole settled window — the pattern DESCENDS as the tether grows, so this is mostly that descent [deg]
    elevation_span_lap_deg: 7.8     # flown elevation span per lobe, which is the pattern's own height [deg]
    elevation_span_pct_of_b: 81     # flown span as % of the B commanded where it was flown
  tether_force_N:
    mean: 1312                      # mean tether force, settled window [N]
    std: 196                        # std tether force, settled window [N]
    cv_pct: 14.9                    # coefficient of variation [%]
  steering:
    peak_abs_u_s: 0.32              # peak |rel_steering| commanded
    pct_time_within_2pct_of_peak: 11 # % of time within 2% of peak (saturation)
    hf_std_steering: 0.0079         # high-frequency std of steering (chatter)
    hf_std_turnrate_deg_s: 0.26     # high-frequency std of heading rate [deg/s]
  tape:
    delivered_peak_abs_u_s: 0.319   # peak steering the KCU tape delivered
    commanded_peak_abs_u_s: 0.32    # peak steering commanded
    rate_limited_pct_time: 8        # % of time the tape's rate limit was hit
    rate_limited_peak_per_s: 0.2    # peak tape rate reached [1/s]
    rate_limit_per_s: 0.2           # KCU's configured rate limit [1/s]
reelout:
  v_app_phase4:
    duration_s: 179.3               # phase-4 window length [s]
    mean_m_s: 13.88                 # mean apparent wind speed [m/s]
    min_m_s: 10.7                   # min apparent wind speed [m/s]
    max_m_s: 17.85                  # max apparent wind speed [m/s]
    v_app_ref_m_s: 27.0             # reference apparent wind speed [m/s]
    deviation_pct: -48.6            # mean v_app deviation from v_app_ref [%]
  tether:
    start_m: 150.0                  # tether length at run start [m]
    end_m: 380.0                    # tether length at run end [m]
    target_m: 380.0                 # reelout_l_max target [m]
    stop_reason: "length"           # criterion that ended reel-out: length, laps, or none
    laps_reeled: 9.33               # figure-eight laps completed by the time reel-out ended
  force:
    cf_force_ro: 1.22               # crest factor: peak / mean tether force over the reeling window
  power:
    cf_power_ro: 1.36               # crest factor: peak / mean reel-out power over the reeling window
    duration_s: 184.3               # reeling window length [s]
    n_samples: 16585                # sample count in the reeling window
    energy_kJ: 376.4                # energy over the reeling window [kJ]
    energy_run_kJ: 360.7            # energy over the whole run [kJ]
  winch_state:
    speed_pct: 98.9                 # % of the reeling window in speed control (state 1)
    lower_force_pct: 1.1            # % in the LowerForceController, reeling in (state 0)
    upper_force_pct: 0.0            # % in the UpperForceController, force capped at f_high (state 2)
    n_samples: 16585                # sample count in the reeling window
  ringing:
    n_peaks: 6                      # ring peaks detected above peak_floor
    period_s: 3.64                  # mean peak-to-peak ring period [s]
    zeta: 0.066                     # damping ratio from the peak log decrement
    overshoot_m_s: 0.28             # first ring peak's amplitude above the local trend [m/s]
    duration_s: 20.0                # time until the ring decays below settle_frac of overshoot_m_s [s]
    peak_v_reelout_m_s: 1.61        # raw v_reelout max within ring_span [m/s]
    steady_v_reelout_m_s: 1.5       # mean v_reelout after ring_span [m/s]
traj_opt:
  power:
    predicted_W: 1938               # predicted mean reel-out power of the paths actually flown, weighted by their share of the reeling window [W]
    predicted_initial_W: 1557       # predicted mean reel-out power of the path installed before the run [W]
    predicted_paths:
      t_000.0_s: 1557               # 15% of the reeling window
      t_058.8_s: 1475               # 16% of the reeling window
      t_087.4_s: 1856               # 15% of the reeling window
      t_114.4_s: 2070               # 14% of the reeling window
      t_140.2_s: 2184               # 14% of the reeling window
      t_166.2_s: 2246               # 15% of the reeling window
      t_193.8_s: 2288               # 12% of the reeling window
    measured_W: 2043                # mean reel-out power the run harvested [W]
    ratio: 1.05                     # measured / predicted, against the weighted prediction
  guess:
    a_deg: 30.0                     # width of the guess lemniscate; azimuth spans ±a [deg]
    b_deg: 12.0                     # height of the guess, peak to peak [deg]
    el_center_deg: 29.0             # centre elevation of the guess [deg]
    points: 361                     # points the guess was sent with
    depower_seed_m: 1.6             # power-tape length the solve started from, input_depower ramped with the wind above input_depower_wind_ref; only a seed, the server optimizes it [m]
    depower_optimized:
      t_000.0_s: 1.516              # optimizer's l_dp [m]; rel_depower equivalent 0.288 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_058.8_s: 1.519              # optimizer's l_dp [m]; rel_depower equivalent 0.289 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_087.4_s: 1.497              # optimizer's l_dp [m]; rel_depower equivalent 0.284 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_114.4_s: 1.481              # optimizer's l_dp [m]; rel_depower equivalent 0.281 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_140.2_s: 1.469              # optimizer's l_dp [m]; rel_depower equivalent 0.279 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_166.2_s: 1.464              # optimizer's l_dp [m]; rel_depower equivalent 0.278 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_193.8_s: 1.46               # optimizer's l_dp [m]; rel_depower equivalent 0.277 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
  path:
    points: 360                     # points of the path installed before the run
    points_final: 360               # points of the path at the end; differs when reopt installed one
    az_center_deg: 1.8              # centre azimuth of the path [deg]
    az_amplitude_deg: 26.2          # half-width of the STARTUP path; the criteria are scored against the pattern commanded at each lap, whose mean is lift_budget's reference [deg]
    el_center_deg: 25.7             # centre elevation of the path [deg]
    el_height_deg: 15.1             # elevation span of the path, peak to peak [deg]
    downloops: true                 # traversal direction the optimizer solved for
  feasibility:
    min_required: 0.82              # min_feasibility_margin of data/traj_opt.yaml [-]
    turn_radius_headroom: 1.2       # factor on the gate's number for what the GATE adds: its finite-difference curvature estimate and the elevation lift [-]
    turn_radius_request_m: 12.68    # minimum turn radius asked of the optimizer for the LAST request; the gate's margin/(c1*max_steering) times the headroom and the lap's reel-out ratio, since the optimizer measures the radius up the reel-out while the run flies the curve at the anchor [m]
    turn_radius_scale: 1.358        # the two corrections together, as sent [-]
    turn_radius_lap_reelout_m: 0.0  # reel-out per lap ASSUMED for the startup request, the only one with no reply to measure it off [m]
    margin_start: 0.93              # curvature margin at the starting length; the worst case only when one path is flown throughout — see the docstring [-]
    margin_end: 2.35                # curvature margin at reelout_l_max [-]
    c1_final: 0.1919                # turn-rate gain at depower_final [1/m]
    margin_final: 1.61              # curvature margin phase 5 flies with: depower_final's c1, at reelout_l_max, on the STARTING path lifted by el_offset_final [-]
    margin_final_flown: 0.78        # the same, for the last path the re-optimizer installed — the one phase 5 actually inherited [-]
  reopt:
    enabled: true                   # re-optimization during the run
    warm_start: true                # /step alone, seeded from the previous optimum; false re-inits from the parametric guess at every length
    requests: 6                     # solves that completed, accepted or rejected
    blocking: true                  # simulation held while a solve ran
    blocked_s: 27.2                 # wall time the simulation was frozen waiting for replies [s]
    installed: 6                    # new paths actually flown
    blend_retries: 0                # cold-restart attempts spent on a rejected reply — a folded blend, a collapsed prediction, or a clearance/elevation shortfall re-asked at a raised floor — across every install this run
    events:
      t_058.8_s: "installed — margin 0.97 (phase 5: 1.78 at 380 m), clearance 46.5 m, 1475 W predicted" # at L = 144 m
      t_087.4_s: "installed — margin 1.05 (phase 5: 1.51 at 380 m), clearance 50.2 m, 1856 W predicted" # at L = 183 m
      t_114.4_s: "installed — margin 1.04 (phase 5: 1.22 at 380 m), clearance 52.6 m, 2070 W predicted" # at L = 224 m
      t_140.2_s: "installed — margin 1.03 (phase 5: 1.03 at 380 m), clearance 53.5 m, 2184 W predicted" # at L = 264 m
      t_166.2_s: "installed — margin 1.02 (phase 5: 0.88 at 380 m), clearance 59.6 m, 2246 W predicted" # at L = 305 m
      t_193.8_s: "installed — margin 1.03 (phase 5: 0.78 at 380 m), clearance 66.9 m, 2288 W predicted" # at L = 348 m
  el_bias:
    gain: 0.5                       # per-lap learning gain for the elevation bias; 0 = off
    final_deg: 1.98                 # mean elevation correction added to the last installed path [deg]
    bins: 5                         # el_bias_bins, azimuth bands the correction is learnt over; 1 = one rigid shift
    smooth: 0.25                    # el_bias_smooth, how far each band is pulled towards its neighbours after every lap [-]
    profile_deg: [1.01, 1.3, 2.17, 3.04, 2.37] # the learnt correction per band, crossing first [deg]
    lift_deg: 1.5                   # el_offset_final, the fixed lift added on top of the learnt bias [deg]
    lift_lead_s: 8.0                # el_offset_lead, how early the lift is allowed to latch; 0 = at the end [s]
    wing_deg: 1.5                   # el_offset_wing, extra lift at the lobes, baked into every installed path [deg]
    wing_mode: "azimuth"            # el_offset_wing_mode, the coordinate that lift is shaped over
    wing_az_deg: 10.0               # azimuth mode: azimuth beyond which that lift is full; it ramps over el_offset_wing_blend below it [deg]
    wing_depth: 0.0                 # elevation mode: depth below the path's elevation centre where the lift starts, in half-spans; full at its lowest point [-]
    lift_t_s: 205.9                 # when the lift actually latched [s]
    lift_remaining_m: 12.4          # reel-out left at that moment; > 0 means the lead fired, 0 means phase 5 did [m]
    shift_delivery:
      t_062.8_s: "blended in"       # +0.54° of shift (mean over the bands), curvature margin 1.08 vs 0.82 required
      t_091.4_s: "blended in"       # +0.27° of shift (mean over the bands), curvature margin 1.05 vs 0.82 required
      t_118.4_s: "blended in"       # -0.16° of shift (mean over the bands), curvature margin 1.07 vs 0.82 required
      t_144.2_s: "blended in"       # -0.06° of shift (mean over the bands), curvature margin 1.02 vs 0.82 required
      t_170.2_s: "blended in"       # +0.04° of shift (mean over the bands), curvature margin 1.05 vs 0.82 required
      t_197.8_s: "blended in"       # +0.13° of shift (mean over the bands), curvature margin 1.05 vs 0.82 required
      t_205.9_s: "blended in"       # +1.50° of shift (mean over the bands), curvature margin 1.09 vs 0.82 required
      t_252.4_s: "held back"        # +1.19° of shift (mean over the bands), curvature margin 0.72 vs 0.82 required
    laps:
      lap_1: -1.05                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.05°, correction became [+0.22 +0.34 +0.58 +0.84 +0.70]° (crossing … lobe)
      lap_2: -0.48                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.99°, correction became [+0.26 +0.45 +0.90 +1.37 +1.04]° (crossing … lobe)
      lap_3: 0.33                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.45°, correction became [+0.13 +0.26 +0.70 +1.18 +0.96]° (crossing … lobe)
      lap_4: 0.14                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.48°, correction became [+0.02 +0.16 +0.65 +1.18 +0.90]° (crossing … lobe)
      lap_5: -0.06                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.61°, correction became [-0.04 +0.12 +0.72 +1.32 +0.99]° (crossing … lobe)
      lap_6: -0.23                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.78°, correction became [-0.02 +0.17 +0.87 +1.53 +1.18]° (crossing … lobe)
      lap_8: -1.12                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.75°, correction became [+0.98 +1.27 +2.14 +2.97 +2.34]° (crossing … lobe)
      lap_9: 0.01                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.78°, correction became [+1.01 +1.30 +2.17 +3.04 +2.37]° (crossing … lobe)
    lap_profiles:
      lap_1: "-0.39 -0.68 -1.15 -1.88 -1.27" # error vs the optimizer's curve per band, crossing first; 394/432/405/328/469 samples, correction now [+0.22 +0.34 +0.58 +0.84 +0.70]° (crossing … lobe)
      lap_2: "-0.03 -0.20 -0.64 -1.43 -0.38" # error vs the optimizer's curve per band, crossing first; 545/583/549/392/509 samples, correction now [+0.26 +0.45 +0.90 +1.37 +1.04]° (crossing … lobe)
      lap_3: "+0.31 +0.43 +0.44 +0.07 +0.35" # error vs the optimizer's curve per band, crossing first; 470/532/538/407/478 samples, correction now [+0.13 +0.26 +0.70 +1.18 +0.96]° (crossing … lobe)
      lap_4: "+0.24 +0.27 +0.10 -0.35 +0.37" # error vs the optimizer's curve per band, crossing first; 465/516/530/400/417 samples, correction now [+0.02 +0.16 +0.65 +1.18 +0.90]° (crossing … lobe)
      lap_5: "+0.14 +0.17 -0.13 -0.71 +0.14" # error vs the optimizer's curve per band, crossing first; 481/532/548/407/372 samples, correction now [-0.04 +0.12 +0.72 +1.32 +0.99]° (crossing … lobe)
      lap_6: "-0.01 -0.00 -0.31 -0.88 -0.07" # error vs the optimizer's curve per band, crossing first; 531/588/574/399/386 samples, correction now [-0.02 +0.17 +0.87 +1.53 +1.18]° (crossing … lobe)
      lap_8: "-0.97 -1.06 -1.28 -1.78 -0.88" # error vs the optimizer's curve per band, crossing first; 600/717/603/249/518 samples, correction now [+0.98 +1.27 +2.14 +2.97 +2.34]° (crossing … lobe)
      lap_9: "-0.00 +0.02 -0.03 -0.42 +0.26" # error vs the optimizer's curve per band, crossing first; 603/718/603/249/524 samples, correction now [+1.01 +1.30 +2.17 +3.04 +2.37]° (crossing … lobe)
  droop_profile:
    az_00_20_pct: 3.66              # kite below the path's elevation centre [deg], over 4939 samples; the path itself sits 0.74 half-spans down there and the kite -0.35° under it
    az_20_40_pct: 2.65              # kite below the path's elevation centre [deg], over 5621 samples; the path itself sits 0.48 half-spans down there and the kite -0.60° under it
    az_40_60_pct: 1.65              # kite below the path's elevation centre [deg], over 5205 samples; the path itself sits 0.07 half-spans down there and the kite -1.32° under it
    az_60_80_pct: 2.02              # kite below the path's elevation centre [deg], over 3205 samples; the path itself sits -0.05 half-spans down there and the kite -2.13° under it
    az_80_100_pct: 1.91             # kite below the path's elevation centre [deg], over 4232 samples; the path itself sits 0.13 half-spans down there and the kite -1.37° under it
    centre_to_lobe_deg: 0.0         # how much deeper the kite flies in its worst azimuth band than in the crossing — what a SHAPED lift is aimed at, where el_bias and el_offset_final can only move the pattern rigidly [deg]
  lift_budget:
    delivered_deg: 2.25             # mean correction the path in the air actually carries — el_bias plus el_offset_final once it has been delivered [deg]
    delivered_profile_deg: [1.48, 1.67, 2.37, 3.03, 2.68] # the same, per azimuth band, crossing first [deg]
    commanded_az_amp_deg: 20.31     # mean half-width the run was COMMANDED to fly, against the startup path's in traj_opt.path [deg]
    commanded_el_height_deg: 10.14  # mean commanded pattern height [deg]
    wing_deg: 1.5                   # el_offset_wing, the fixed lobe lift baked into every installed path [deg]
    el_min_run_deg: 9.8             # lowest elevation over the whole run — set in phase 4, so a phase-5 lift does not move it [deg]
    el_min_final_deg: 9.76          # lowest elevation in phase 5, which is what el_offset_final buys [deg]
    azimuth_reach_pos_deg: 4.87     # reach margin: flew 19.09° against the 14.22° required by min_span_frac = 0.70, +34 %
    azimuth_reach_neg_deg: 5.07     # reach margin: flew 19.28° against the 14.22° required by min_span_frac = 0.70, +36 %
    elevation_span_deg: 1.11        # reach margin: flew 8.21° against the 7.10° required by min_span_frac = 0.70, +16 %
    tightest: "elevation span"      # the size criterion with the least room, +1.11° (+16 %) — what the NEXT lift has to spend
  clearance:
    path_min_m: 46.7                # lowest point of the PRE-FLIGHT path at the starting length [m]
    flown_min_m: 33.2               # lowest point the kite actually reached over the run [m]
    min_required_m: 40.0            # min_height of data/traj_opt.yaml [m]
    elevation_min_asked_deg: 9.5    # elevation floor the LAST request carried, the higher of asind(min_height/L) and min_elevation + candidate_elevation_margin at the length it was made for; the optimizer constrains HEIGHT, and only at the far end of the lap's reel-out [deg]
    elevation_min_extra_deg: 0.0    # raise a rejected reply's shortfall added to that floor, carried forward once measured; 0 means no reply was gated out low [deg]
  inflow:
    wind_speed_m_s: 4.0             # wind speed at 6 m sent to the optimizer [m/s]
    wind_direction_deg: 270.0       # direction the wind comes from [deg]
    profile_law: 3                  # 0=CONST, 1=EXP, 2=LOG, 3=EXPLOG, 4-6=CUSTOM_*
  winch:
    k_v: 0.0408                     # v_set = k_v * sqrt(force) sent to the optimizer [-]
    f_min_N: 700.0                  # minimum winch force sent [N]
    f_max_N: 8000.0                 # maximum winch force sent [N]
performance:
  sim_time_s: 294.0                 # simulated time [s]
  wall_time_s: 105.5                # wall-clock time [s]
  total_wall_time_s: 228.7          # the whole script: package loading, init, settling, the startup solve and the run, up to this summary — the archive copy and any plots follow it [s]
  optimization_time_s: 36.6         # wall time waiting for the optimizer: the startup solve (9.4 s) plus every blocking re-optimization (traj_opt.reopt.blocked_s) [s]
  realtime_factor: 3.75             # sim_time / wall_time, excluding time frozen for re-optimization
  ms_per_step: 3.0                  # wall time per step, excluding time frozen for re-optimization [ms]
  steps: 26463                      # step count
  dt_s: 0.011111111111111112        # simulation timestep [s]
  vsm_interval: 10                  # VSM aerodynamic update interval [steps]
summary:
  date: "2026-08-25"                # wall-clock date the run finished
  time: "19:49:37"                  # wall-clock time the run finished
  wind_speed_gnd: 4.0               # wind speed at 6 m sent to the optimizer [m/s]
  power_ratio: 1.05                 # measured / predicted mean reel-out power, against the weighted prediction
  total_wall_time_s: 228.7          # the whole script, start to this summary [s]
  realtime_factor: 3.75             # sim_time / wall_time, excluding time frozen for re-optimization
  optimization_requests: 6          # solves that completed, accepted or rejected
  optimizations_installed: 6        # new paths actually flown
  av_power_ro: 2075                 # mean reel-out power over phase four [W]
  min_power_ro: 29                  # min reel-out power over phase four [W]
  max_power_ro: 2787                # max reel-out power over phase four [W]
  min_force_ro: 853                 # min tether force over phase four [N]
  av_force_ro: 1375                 # mean tether force over phase four [N]
  max_force_ro: 1670                # max tether force over phase four [N]
  v_ro_min: 0.02                    # min reel-out speed over phase four [m/s]
  v_ro_av: 1.49                     # mean reel-out speed over phase four [m/s]
  v_ro_max: 1.67                    # max reel-out speed over phase four [m/s]
  av_depower_ro: 0.283              # mean KCU depower over phase four [-]
