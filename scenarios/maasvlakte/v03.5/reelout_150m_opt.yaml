# Run summary for output/reelout_150m_opt.arrow, written by examples/simple_reelout.jl at the end of the run.
success_criteria: "all 10 passed"   # pass/fail verdict vs V3Kite's success criteria
simulation:
  script: "simple_opt_reelout.jl"   # script that produced this run
  project: "system_reelout_maasvlakte.yaml" # system project flown
  rel_turbulence: 0.0               # turbulence level in [0, 1] passed to init
  wind_speed: 3.5                   # mean wind speed in m/s passed to init
  time: "22:38:14"                  # wall-clock time the run finished
  date: "2026-09-20"                # wall-clock date the run finished
  hostname: "framework"             # machine the run executed on
  git_hash: "7c18d4e"               # SimpleKiteControllers.jl commit hash
  git_status: "dirty"               # SimpleKiteControllers.jl working tree: clean or dirty
fig8_metrics:
  settled_from: 29.4                # sim time the scoring window begins [s]
  settle_time: 27.4                 # time after t_start to converge [s]
  laps: 7.0                         # figure-eight laps completed
  lap_time:
    fastest: 33.3                   # shortest time for one full figure of eight — lap 1, starting at t = 30.6 s [s]
    mean: 45.4                      # mean time per full figure of eight, over 7 laps [s]
    slowest: 54.8                   # longest time for one full figure of eight — lap 7 [s]
  cross_track_deg:
    rms: 0.79                       # RMS cross-track error vs the unlifted optimizer path [deg]
    mean: 0.58                      # mean cross-track error vs the unlifted optimizer path [deg]
    max: 4.96                       # max cross-track error vs the unlifted optimizer path [deg]
  elevation_deg:
    min_settled: 10.7               # min elevation, settled window [deg]
    min_whole_run: 10.5             # min elevation, whole run [deg]
  peak_turn_rate_deg_s: 55          # peak heading rate [deg/s]
  extent:
    azimuth_deg:
      min: -22.1                    # flown azimuth reach, negative side [deg]
      max: 21.9                     # flown azimuth reach, positive side [deg]
    azimuth_pct_of_amplitude:
      min: 97                       # negative reach as % of the pattern's ±A
      max: 100                      # positive reach as % of the pattern's ±A
    worst_lobe_deg:
      min: -17.6                    # weakest lobe, negative side [deg]
      max: 16.6                     # weakest lobe, positive side [deg]
    elevation_span_deg: 25.0        # flown elevation span over the whole settled window — the pattern DESCENDS as the tether grows, so this is mostly that descent [deg]
    elevation_span_lap_deg: 12.7    # flown elevation span per lobe, which is the pattern's own height [deg]
    elevation_span_pct_of_b: 81     # flown span as % of the B commanded where it was flown
  tether_force_N:
    mean: 768                       # mean tether force, settled window [N]
    std: 200                        # std tether force, settled window [N]
    cv_pct: 26.0                    # coefficient of variation [%]
  steering:
    peak_abs_u_s: 0.32              # peak |rel_steering| commanded
    pct_time_within_2pct_of_peak: 7 # % of time within 2% of peak (saturation)
    hf_std_steering: 0.0136         # high-frequency std of steering (chatter)
    hf_std_turnrate_deg_s: 0.15     # high-frequency std of heading rate [deg/s]
  tape:
    delivered_peak_abs_u_s: 0.32    # peak steering the KCU tape delivered
    commanded_peak_abs_u_s: 0.32    # peak steering commanded
    rate_limited_pct_time: 4        # % of time the tape's rate limit was hit
    rate_limited_peak_per_s: 0.2    # peak tape rate reached [1/s]
    rate_limit_per_s: 0.2           # KCU's configured rate limit [1/s]
reelout:
  v_app_phase4:
    duration: 296.9                 # phase-4 window length [s]
    mean_m_s: 10.49                 # mean apparent wind speed [m/s]
    min_m_s: 7.09                   # min apparent wind speed [m/s]
    max_m_s: 16.43                  # max apparent wind speed [m/s]
    v_app_ref_m_s: 27.0             # reference apparent wind speed [m/s]
    deviation_pct: -61.2            # mean v_app deviation from v_app_ref [%]
  tether:
    start_m: 150.0                  # tether length at run start [m]
    end_m: 380.0                    # tether length at run end [m]
    target_m: 380.0                 # reelout_l_max target [m]
    stop_reason: "length"           # criterion that ended reel-out: length, laps, or none
    laps_reeled: 7.12               # figure-eight laps completed by the time reel-out ended
  force:
    cf_force_ro: 1.74               # crest factor: peak / mean tether force over the reeling window
  power:
    cf_power_ro: 2.47               # crest factor: peak / mean reel-out power over the reeling window
    duration: 308.5                 # reeling window length [s]
    n_samples: 27768                # sample count in the reeling window
    energy_kJ: 219.4                # energy over the reeling window [kJ]
    energy_run_kJ: 205.5            # energy over the whole run [kJ]
  winch_state:
    speed_pct: 100.0                # % of the reeling window in speed control (state 1)
    lower_force_pct: 0.0            # % in the LowerForceController, reeling in (state 0)
    upper_force_pct: 0.0            # % in the UpperForceController, force capped at f_high (state 2)
    n_samples: 27768                # sample count in the reeling window
  ringing:
    n_peaks: 3                      # ring peaks detected above peak_floor
    period: 6.17                    # mean peak-to-peak ring period [s]
    zeta: 0.08                      # damping ratio from the peak log decrement
    overshoot_m_s: 0.11             # first ring peak's amplitude above the local trend [m/s]
    duration: 20.0                  # time until the ring decays below settle_frac of overshoot_m_s [s]
    peak_v_reelout_m_s: 1.12        # raw v_reelout max within ring_span [m/s]
    steady_v_reelout_m_s: 0.87      # mean v_reelout after ring_span [m/s]
traj_opt:
  power:
    predicted_W: 554                # predicted mean reel-out power of the paths actually flown, weighted by their share of the reeling window [W]
    predicted_initial_W: 494        # predicted mean reel-out power of the path installed before the run [W]
    predicted_paths:
      t_000.0_s: 494                # 29% of the reeling window
      t_107.9_s: 438                # 14% of the reeling window
      t_150.6_s: 559                # 16% of the reeling window
      t_200.1_s: 625                # 16% of the reeling window
      t_250.6_s: 638                # 16% of the reeling window
      t_300.9_s: 637                # 9% of the reeling window
    measured_W: 711                 # mean reel-out power the run harvested [W]
    ratio: 1.28                     # measured / predicted, against the weighted prediction
    free_speed_reference_W: 750     # mean reel-out power an OPTIMAL path could harvest with ANY winch in [f_min, f_max], solved after the run at 6 lengths and weighted by time spent at each. An UPPER BOUND, not a prediction of this k_v law, and never flown [W]
    free_speed_points:
      L_115.5_m: -29                # free_speed solve [W]
      L_168.4_m: 812                # free_speed solve [W]
      L_221.3_m: 874                # free_speed solve [W]
      L_274.2_m: 884                # free_speed solve [W]
      L_327.1_m: 865                # free_speed solve [W]
      L_380.0_m: 831                # free_speed solve [W]
    free_speed_ratio: 0.95          # measured / free_speed_reference_W; above 1 means the run harvests more than the optimizer's own upper bound, which no winch law can explain
  guess:
    a_deg: 30.0                     # width of the guess lemniscate; azimuth spans ±a [deg]
    b_deg: 12.0                     # height of the guess, peak to peak [deg]
    el_center_deg: 29.0             # centre elevation of the guess the startup path was solved from; guess_el_center_high at and above guess_el_center_wind_ref, plus the retry offset below [deg]
    el_center_retry_offset_deg: 0.0 # offset of the seed the startup solve converged from after a 422 — a startup_retry_el_offsets entry, or a whole degree walked outward past them when the failure cache rejected the listed ones; 0 means the shipped guess converged [deg]
    points: 361                     # points the guess was sent with
    depower_seed_m: 1.6             # power-tape length the solve started from, input_depower ramped with the wind above input_depower_wind_ref; only a seed, the server optimizes it [m]
    depower_optimized:
      t_000.0_s: 1.58               # optimizer's l_dp [m]; rel_depower equivalent 0.297 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_107.9_s: 1.568              # optimizer's l_dp [m]; rel_depower equivalent 0.295 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_150.6_s: 1.563              # optimizer's l_dp [m]; rel_depower equivalent 0.294 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_200.1_s: 1.562              # optimizer's l_dp [m]; rel_depower equivalent 0.293 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_250.6_s: 1.561              # optimizer's l_dp [m]; rel_depower equivalent 0.293 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_300.9_s: 1.562              # optimizer's l_dp [m]; rel_depower equivalent 0.293 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
    depower_optimized_rel:
      t_000.0_s: 0.297              # the same reply as V3Kite rel_depower, converted with the AWETRIM_V3KITE_DEPOWER_OFFSET in force at run time; what a replot's u_d panel draws [-]
      t_107.9_s: 0.2946             # the same reply as V3Kite rel_depower, converted with the AWETRIM_V3KITE_DEPOWER_OFFSET in force at run time; what a replot's u_d panel draws [-]
      t_150.6_s: 0.2935             # the same reply as V3Kite rel_depower, converted with the AWETRIM_V3KITE_DEPOWER_OFFSET in force at run time; what a replot's u_d panel draws [-]
      t_200.1_s: 0.2935             # the same reply as V3Kite rel_depower, converted with the AWETRIM_V3KITE_DEPOWER_OFFSET in force at run time; what a replot's u_d panel draws [-]
      t_250.6_s: 0.2933             # the same reply as V3Kite rel_depower, converted with the AWETRIM_V3KITE_DEPOWER_OFFSET in force at run time; what a replot's u_d panel draws [-]
      t_300.9_s: 0.2935             # the same reply as V3Kite rel_depower, converted with the AWETRIM_V3KITE_DEPOWER_OFFSET in force at run time; what a replot's u_d panel draws [-]
    k_v_optimized:
  path:
    points: 360                     # points of the path installed before the run
    points_final: 360               # points of the path at the end; differs when reopt installed one
    az_center_deg: -0.9             # centre azimuth of the path [deg]
    az_amplitude_deg: 23.6          # half-width of the STARTUP path; the criteria are scored against the pattern commanded at each lap, whose mean is lift_budget's reference [deg]
    el_center_deg: 29.3             # centre elevation of the path [deg]
    el_height_deg: 16.9             # elevation span of the path, peak to peak [deg]
    lobe_lift_pct: 75               # share of el_offset_wing the STARTUP install carried; held back on the same rungs as a mid-run install when the full lift fails the curvature gate
    seed_spread_pct: 0              # share of the remembered bias's band-to-band SPREAD the STARTUP install carried; its mean always goes in whole, and the spread is rationed before the lobe lift is
    downloops: true                 # traversal direction the optimizer solved for
  feasibility:
    min_required: 0.82              # min_feasibility_margin of data/traj_opt.yaml [-]
    turn_radius_headroom: 1.25      # factor on the gate's number for what the GATE adds: its finite-difference curvature estimate and the elevation lift [-]
    turn_radius_request_m: 15.49    # minimum turn radius asked of the optimizer for the LAST request; the gate's margin/(c1*max_steering) times the headroom and the lap's reel-out ratio, since the optimizer measures the radius up the reel-out while the run flies the curve at the anchor [m]
    turn_radius_scale: 1.342        # the two corrections together, as sent [-]
    turn_radius_lap_reelout_m: 0.0  # reel-out per lap ASSUMED for the startup request, the only one with no reply to measure it off [m]
    c1_pattern: 0.2201              # turn-rate gain the startup gate and requests read the path at: at the depower the pattern is FLOWN at (the optimizer's own under fly_opt_depower), not depower_setpoint's [1/m]
    gain_scale_flown: 1.105         # heading_p factor phases 3-4 flew with, c1(depower_setpoint)/c1(flown) for the startup reply; 1.0 when the optimizer's depower is the setpoint [-]
    margin_start: 0.87              # curvature margin at the starting length; the worst case only when one path is flown throughout — see the docstring [-]
    margin_end: 2.2                 # curvature margin at reelout_l_max [-]
    c1_final: 0.1655                # turn-rate gain at depower_final [1/m]
    margin_final: 1.55              # curvature margin phase 5 flies with: depower_final's c1, at reelout_l_max, on the STARTING path lifted by el_offset_final [-]
    margin_final_flown: 0.86        # the same, for the last path the re-optimizer installed — the one phase 5 actually inherited [-]
  reopt:
    enabled: true                   # re-optimization during the run
    warm_start: true                # /step alone, seeded from the previous optimum; false re-inits from the parametric guess at every length
    requests: 6                     # solves that completed, accepted or rejected
    blocking: true                  # simulation held while a solve ran
    blocked: 93.7                   # wall time the simulation was frozen waiting for replies [s]
    installed: 5                    # new paths actually flown
    cycle_wall:
      t_063.9_s: 58.3               # wall time from the first request to the verdict (failed), every retry included, at L = 149 m [s]
      t_107.9_s: 12.7               # wall time from the first request to the verdict (installed), every retry included, at L = 185 m [s]
      t_150.6_s: 6.1                # wall time from the first request to the verdict (installed), every retry included, at L = 223 m [s]
      t_200.1_s: 6.0                # wall time from the first request to the verdict (installed), every retry included, at L = 266 m [s]
      t_250.6_s: 5.5                # wall time from the first request to the verdict (installed), every retry included, at L = 313 m [s]
      t_300.9_s: 6.0                # wall time from the first request to the verdict (installed), every retry included, at L = 358 m [s]
    blend_retries: 0                # cold-restart attempts spent on a rejected reply — a folded blend, a collapsed prediction, or a clearance/elevation shortfall re-asked at a raised floor — across every install this run
    events:
      t_063.9_s: "failed"           # at L = 149 m
      t_107.9_s: "installed — margin 0.86 (droop spread at 75 %, lobe lift at 0 %) (phase 5: 1.31 at 380 m), clearance 50.6 m, 438 W predicted" # at L = 185 m
      t_150.6_s: "installed — margin 1.07 (phase 5: 1.35 at 380 m), clearance 52.7 m, 559 W predicted" # at L = 223 m
      t_200.1_s: "installed — margin 1.08 (phase 5: 1.14 at 380 m), clearance 54.7 m, 625 W predicted" # at L = 266 m
      t_250.6_s: "installed — margin 1.11 (phase 5: 0.99 at 380 m), clearance 66.4 m, 638 W predicted" # at L = 313 m
      t_300.9_s: "installed — margin 1.09 (phase 5: 0.86 at 380 m), clearance 77.9 m, 637 W predicted" # at L = 358 m
  el_bias:
    learning: true                  # traj_opt.learning; false zeroes the gains and leaves the cache alone
    gain: 0.5                       # per-lap learning gain for the elevation bias; 0 = off
    final_deg: 1.2                  # mean elevation correction added to the last installed path [deg]
    bins: 5                         # el_bias_bins, azimuth bands the correction is learnt over; 1 = one rigid shift
    smooth: 0.25                    # el_bias_smooth, how far each band is pulled towards its neighbours after every lap [-]
    profile_deg: [0.89, 0.92, 1.19, 1.75, 1.26] # the learnt correction per band, crossing first [deg]
    seed_deg: [1.08, 1.2, 2.05, 2.29, 1.55] # the correction the run started from, remembered per project and wind speed after el_bias_seed_laps laps of the previous run; zeros = none
    seed_laps: 2                    # el_bias_seed_laps, laps after which this run's correction is remembered; 0 = off
    lift_deg: 3.0                   # el_offset_final, the fixed lift added on top of the learnt bias [deg]
    lift_lead: 8.0                  # el_offset_lead, how early the lift is allowed to latch; 0 = at the end [s]
    wing_deg: 1.5                   # el_offset_wing, extra lift at the lobes, baked into every installed path [deg]
    wing_mode: "azimuth"            # el_offset_wing_mode, the coordinate that lift is shaped over
    wing_az_deg: 10.0               # azimuth mode: azimuth beyond which that lift is full; it ramps over el_offset_wing_blend below it [deg]
    wing_depth: 0.0                 # elevation mode: depth below the path's elevation centre where the lift starts, in half-spans; full at its lowest point [-]
    lift_t: 316.2                   # when the lift actually latched [s]
    lift_remaining_m: 10.3          # reel-out left at that moment; > 0 means the lead fired, 0 means phase 5 did [m]
    shift_delivery:
      t_030.6_s: "held back"        # +0.00° of shift (mean over the bands), curvature margin 0.34 vs 0.82 required
      t_063.9_s: "blended in (100 % of the mean, 0 % of the spread)" # +0.11° of shift (mean over the bands), curvature margin 0.91 vs 0.82 required
      t_067.9_s: "held back"        # +0.00° of shift (mean over the bands), curvature margin 0.42 vs 0.82 required
      t_099.8_s: "blended in (100 % of the mean, 25 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_103.9_s: "blended in (100 % of the mean, 0 % of the spread)" # -0.09° of shift (mean over the bands), curvature margin 0.85 vs 0.82 required
      t_107.9_s: "carried by an install (75 % of the spread)" # -0.00° of shift (mean over the bands), curvature margin 0.86 vs 0.82 required
      t_111.9_s: "blended in (100 % of the mean, 50 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_115.9_s: "blended in (100 % of the mean, 25 % of the spread)" # -0.00° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_119.9_s: "blended in (100 % of the mean, 25 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.83 vs 0.82 required
      t_124.0_s: "blended in"       # -0.00° of shift (mean over the bands), curvature margin 0.83 vs 0.82 required
      t_144.5_s: "blended in"       # -0.28° of shift (mean over the bands), curvature margin 0.88 vs 0.82 required
      t_193.5_s: "blended in"       # -0.09° of shift (mean over the bands), curvature margin 1.26 vs 0.82 required
      t_243.5_s: "blended in"       # -0.07° of shift (mean over the bands), curvature margin 1.25 vs 0.82 required
      t_293.7_s: "blended in"       # -0.01° of shift (mean over the bands), curvature margin 1.20 vs 0.82 required
      t_316.2_s: "blended in"       # +3.00° of shift (mean over the bands), curvature margin 1.14 vs 0.82 required
    laps:
      lap_1: -0.1                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.59°, correction became [+1.09 +1.23 +2.16 +2.54 +1.71]° (crossing … lobe)
      lap_2: 0.19                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.37°, correction became [+1.09 +1.22 +2.07 +2.31 +1.58]° (crossing … lobe)
      lap_3: 0.68                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.93°, correction became [+0.97 +0.94 +1.47 +2.05 +1.44]° (crossing … lobe)
      lap_4: 0.22                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.04°, correction became [+0.98 +0.93 +1.30 +1.89 +1.34]° (crossing … lobe)
      lap_5: 0.19                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.03°, correction became [+0.88 +0.87 +1.20 +1.81 +1.32]° (crossing … lobe)
      lap_6: 0.04                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.10°, correction became [+0.89 +0.92 +1.19 +1.75 +1.26]° (crossing … lobe)
    lap_profiles:
      lap_1: "-0.10 +0.16 -0.43 -1.09 +0.33" # error vs the optimizer's curve per band, crossing first; 786/862/472/319/559 samples, correction now [+1.09 +1.23 +2.16 +2.54 +1.71]° (crossing … lobe)
      lap_2: "-0.06 +0.24 -0.08 +0.01 +0.82" # error vs the optimizer's curve per band, crossing first; 962/1000/512/356/591 samples, correction now [+1.09 +1.22 +2.07 +2.31 +1.58]° (crossing … lobe)
      lap_3: "+0.10 +0.70 +1.22 -0.04 +0.81" # error vs the optimizer's curve per band, crossing first; 709/935/1111/382/692 samples, correction now [+0.97 +0.94 +1.47 +2.05 +1.44]° (crossing … lobe)
      lap_4: "-0.12 +0.11 +0.43 -0.21 +0.68" # error vs the optimizer's curve per band, crossing first; 753/1368/1174/374/739 samples, correction now [+0.98 +0.93 +1.30 +1.89 +1.34]° (crossing … lobe)
      lap_5: "+0.10 +0.16 +0.31 -0.36 +0.49" # error vs the optimizer's curve per band, crossing first; 935/1072/1300/483/717 samples, correction now [+0.88 +0.87 +1.20 +1.81 +1.32]° (crossing … lobe)
      lap_6: "-0.06 -0.08 +0.13 -0.37 +0.53" # error vs the optimizer's curve per band, crossing first; 917/1132/1289/499/674 samples, correction now [+0.89 +0.92 +1.19 +1.75 +1.26]° (crossing … lobe)
  droop_profile:
    az_00_20_pct: 6.56              # kite below the path's elevation centre [deg], over 6207 samples; the path itself sits 0.74 half-spans down there and the kite -1.08° under it
    az_20_40_pct: 3.95              # kite below the path's elevation centre [deg], over 7890 samples; the path itself sits 0.42 half-spans down there and the kite -1.01° under it
    az_40_60_pct: 1.11              # kite below the path's elevation centre [deg], over 7470 samples; the path itself sits -0.02 half-spans down there and the kite -1.31° under it
    az_60_80_pct: 2.2               # kite below the path's elevation centre [deg], over 2884 samples; the path itself sits -0.03 half-spans down there and the kite -2.41° under it
    az_80_100_pct: -0.0             # kite below the path's elevation centre [deg], over 4774 samples; the path itself sits -0.12 half-spans down there and the kite -0.96° under it
    centre_to_lobe_deg: 0.0         # how much deeper the kite flies in its worst azimuth band than in the crossing — what a SHAPED lift is aimed at, where el_bias and el_offset_final can only move the pattern rigidly [deg]
  lift_budget:
    delivered_deg: 4.2              # mean correction the path in the air actually carries — el_bias plus el_offset_final once it has been delivered [deg]
    delivered_profile_deg: [3.89, 3.92, 4.19, 4.75, 4.26] # the same, per azimuth band, crossing first [deg]
    commanded_az_amp_deg: 21.89     # mean half-width the run was COMMANDED to fly, against the startup path's in traj_opt.path [deg]
    commanded_el_height_deg: 14.68  # mean commanded pattern height [deg]
    wing_deg: 1.5                   # el_offset_wing, the fixed lobe lift baked into every installed path [deg]
    el_min_run_deg: 10.5            # lowest elevation over the whole run — usually set in PHASE 5 at 4 m/s and up, so el_offset_final does move it; the entry sets it below that [deg]
    el_min_final_deg: 13.37         # lowest elevation in phase 5, which is what el_offset_final buys [deg]
    azimuth_reach_pos_deg: 6.47     # reach margin: flew 21.79° against the 15.32° required by min_span_frac = 0.70, +42 %
    azimuth_reach_neg_deg: 5.84     # reach margin: flew 21.16° against the 15.32° required by min_span_frac = 0.70, +38 %
    elevation_span_deg: 1.66        # reach margin: flew 11.94° against the 10.28° required by min_span_frac = 0.70, +16 %
    tightest: "elevation span"      # the size criterion with the least room, +1.66° (+16 %) — what the NEXT lift has to spend
  clearance:
    path_min_m: 53.5                # lowest point of the PRE-FLIGHT path at the starting length [m]
    flown_min_m: 21.4               # lowest point the kite actually reached over the run [m]
    min_required_m: 40.0            # min_height of data/traj_opt.yaml [m]
    elevation_min_asked_deg: 9.5    # elevation floor the LAST request carried, the higher of asind(min_height/L) and min_elevation + candidate_elevation_margin at the length it was made for; the optimizer constrains HEIGHT, and only at the far end of the lap's reel-out [deg]
    elevation_min_extra_deg: 0.0    # raise a rejected reply's shortfall added to that floor, carried forward once measured; 0 means no reply was gated out low [deg]
  inflow:
    wind_speed_m_s: 3.5             # wind speed at 6 m sent to the optimizer [m/s]
    wind_direction_deg: 270.0       # direction the wind comes from [deg]
    profile_law: 3                  # 0=CONST, 1=EXP, 2=LOG, 3=EXPLOG, 4-6=CUSTOM_*
  winch:
    k_v: 0.0408                     # v_set = k_v * sqrt(force) sent to the optimizer [-]
    optimize_k_v: false             # k_v sent as a design variable; the reply's value is then the one flown
    k_v_flown: 0.0408               # k_v the run actually flew; == k_v, the optimizer did not move it
    f_min_N: 525.0                  # minimum winch force sent [N]
    f_max_N: 8000.0                 # maximum winch force sent [N]
    force_limit: "soft"             # the reel-out law itself limits the force, inverting the optimizer's own saturating tension curve (beta 3e-02/1e-03, force filtered at tau = 2.48 s); the UpperForceController is held in reset, so upper_force_pct is 0 by construction
performance:
  sim_time: 355.3                   # simulated time [s]
  wall_time: 232.8                  # wall-clock time [s]
  total_wall_time: 358.0            # the whole script: package loading, init, settling, the startup solve and the run, up to this summary — the archive copy and any plots follow it [s]
  optimization_time: 131.5          # wall time waiting for the optimizer: the startup solve (37.8 s) plus every blocking re-optimization (traj_opt.reopt.blocked) [s]
  max_optimization_time: 58.3       # longest wall time to compute a new figure of eight, retries included: the re-optimization at t = 63.9 s (failed, L = 149 m); the startup solve took 37.8 s [s]
  realtime_factor: 2.55             # sim_time / wall_time, excluding time frozen for re-optimization
  ms_per_step: 4.4                  # wall time per step, excluding time frozen for re-optimization [ms]
  steps: 31979                      # step count
  dt: 0.011111111111111112          # simulation timestep [s]
  vsm_interval: 10                  # VSM aerodynamic update interval [steps]
summary:
  date: "2026-09-20"                # wall-clock date the run finished
  time: "22:38:14"                  # wall-clock time the run finished
  wind_speed_gnd: 3.5               # wind speed at 6 m sent to the optimizer [m/s]
  power_ratio: 1.28                 # measured / predicted reel-out power [-]
  power_ratio_free_speed: 0.95      # measured / free_speed reference reel-out power (750 W, an UPPER BOUND over any winch in [f_min, f_max]); above 1 cannot be explained by any winch law [-]
  success_criteria: "all 10 passed" # pass/fail verdict vs V3Kite's success criteria
  cross_track_rms_deg: 0.79         # RMS cross-track error, settled window [deg]
  total_wall_time: 358.0            # the whole script, start to this summary [s]
  realtime_factor: 2.55             # sim_time / wall_time, excluding time frozen for re-optimization
  optimization_requests: 6          # solves that completed, accepted or rejected
  optimizations_installed: 5        # new paths actually flown
  fastest_fig8: 33.3                # shortest time for flying one full figure of eight [s]
  max_optimization_time: 58.3       # longest wall time to compute a new figure of eight, retries included [s]
  av_power_ro: 725                  # mean reel-out power over phase four [W]
  min_power_ro: 196                 # min reel-out power over phase four, last 2 s excluded [W]
  max_power_ro: 1759                # max reel-out power over phase four [W]
  min_force_ro: 529                 # min tether force over phase four, last 2 s excluded [N]
  av_force_ro: 787                  # mean tether force over phase four [N]
  max_force_ro: 1360                # max tether force over phase four [N]
  v_ro_min: 0.32                    # min reel-out speed over phase four, last 2 s excluded [m/s]
  v_ro_av: 0.87                     # mean reel-out speed over phase four [m/s]
  v_ro_max: 1.35                    # max reel-out speed over phase four [m/s]
  av_depower_ro: 0.295              # mean KCU depower over phase four [-]
  max_depower_final: 0.35           # highest depower the force limiter asked for, from the stop latch through phase 5; the phase-5 floor itself when it never engaged or is off (depower_final_max == depower_final) [-]
  gain_scale_final_peak: 1.469      # heading_p factor phase 5 flew with at the limiter's peak, c1(depower_setpoint)/c1(flown), read at 0.35 [-]
