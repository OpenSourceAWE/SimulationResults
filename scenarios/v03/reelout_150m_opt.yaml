# Run summary for output/reelout_150m_opt.arrow, written by examples/simple_reelout.jl at the end of the run.
success_criteria: "all 10 passed"   # pass/fail verdict vs V3Kite's success criteria
simulation:
  script: "simple_opt_reelout.jl"   # script that produced this run
  project: "system_reelout_150m.yaml" # system project flown
  rel_turbulence: "default"         # turbulence level in [0, 1] passed to init
  wind_speed: 3.0                   # mean wind speed in m/s passed to init
  time: "00:34:33"                  # wall-clock time the run finished
  date: "2026-08-29"                # wall-clock date the run finished
  hostname: "ufryzen"               # machine the run executed on
  git_hash: "d572c57"               # SimpleKiteControllers.jl commit hash
  git_status: "dirty"               # SimpleKiteControllers.jl working tree: clean or dirty
fig8_metrics:
  settled_from_s: 42.0              # sim time the scoring window begins [s]
  settle_time_s: 40.0               # time after t_start to converge [s]
  laps: 9.0                         # figure-eight laps completed
  cross_track_deg:
    rms: 1.8                        # RMS cross-track error [deg]
    mean: 1.47                      # mean cross-track error [deg]
    max: 7.67                       # max cross-track error [deg]
  elevation_deg:
    min_settled: 11.8               # min elevation, settled window [deg]
    min_whole_run: 11.8             # min elevation, whole run [deg]
  peak_turn_rate_deg_s: 59          # peak heading rate [deg/s]
  extent:
    azimuth_deg:
      min: -27.9                    # flown azimuth reach, negative side [deg]
      max: 26.6                     # flown azimuth reach, positive side [deg]
    azimuth_pct_of_amplitude:
      min: 98                       # negative reach as % of the pattern's ±A
      max: 96                       # positive reach as % of the pattern's ±A
    worst_lobe_deg:
      min: -21.5                    # weakest lobe, negative side [deg]
      max: 19.6                     # weakest lobe, positive side [deg]
    elevation_span_deg: 32.4        # flown elevation span over the whole settled window — the pattern DESCENDS as the tether grows, so this is mostly that descent [deg]
    elevation_span_lap_deg: 13.9    # flown elevation span per lobe, which is the pattern's own height [deg]
    elevation_span_pct_of_b: 77     # flown span as % of the B commanded where it was flown
  tether_force_N:
    mean: 502                       # mean tether force, settled window [N]
    std: 183                        # std tether force, settled window [N]
    cv_pct: 36.5                    # coefficient of variation [%]
  steering:
    peak_abs_u_s: 0.32              # peak |rel_steering| commanded
    pct_time_within_2pct_of_peak: 8 # % of time within 2% of peak (saturation)
    hf_std_steering: 0.0153         # high-frequency std of steering (chatter)
    hf_std_turnrate_deg_s: 0.16     # high-frequency std of heading rate [deg/s]
  tape:
    delivered_peak_abs_u_s: 0.32    # peak steering the KCU tape delivered
    commanded_peak_abs_u_s: 0.32    # peak steering commanded
    rate_limited_pct_time: 4        # % of time the tape's rate limit was hit
    rate_limited_peak_per_s: 0.2    # peak tape rate reached [1/s]
    rate_limit_per_s: 0.2           # KCU's configured rate limit [1/s]
reelout:
  v_app_phase4:
    duration_s: 498.8               # phase-4 window length [s]
    mean_m_s: 8.63                  # mean apparent wind speed [m/s]
    min_m_s: 5.12                   # min apparent wind speed [m/s]
    max_m_s: 14.99                  # max apparent wind speed [m/s]
    v_app_ref_m_s: 27.0             # reference apparent wind speed [m/s]
    deviation_pct: -68.0            # mean v_app deviation from v_app_ref [%]
  tether:
    start_m: 150.0                  # tether length at run start [m]
    end_m: 380.0                    # tether length at run end [m]
    target_m: 380.0                 # reelout_l_max target [m]
    stop_reason: "length"           # criterion that ended reel-out: length, laps, or none
    laps_reeled: 9.19               # figure-eight laps completed by the time reel-out ended
  force:
    cf_force_ro: 2.08               # crest factor: peak / mean tether force over the reeling window
  power:
    cf_power_ro: 2.8                # crest factor: peak / mean reel-out power over the reeling window
    duration_s: 445.0               # reeling window length [s]
    n_samples: 40050                # sample count in the reeling window
    energy_kJ: 184.2                # energy over the reeling window [kJ]
    energy_run_kJ: 159.3            # energy over the whole run [kJ]
  winch_state:
    speed_pct: 100.0                # % of the reeling window in speed control (state 1)
    lower_force_pct: 0.0            # % in the LowerForceController, reeling in (state 0)
    upper_force_pct: 0.0            # % in the UpperForceController, force capped at f_high (state 2)
    n_samples: 40050                # sample count in the reeling window
  ringing:
    n_peaks: 3                      # ring peaks detected above peak_floor
    period_s: 6.13                  # mean peak-to-peak ring period [s]
    zeta: 0.134                     # damping ratio from the peak log decrement
    overshoot_m_s: 0.11             # first ring peak's amplitude above the local trend [m/s]
    duration_s: 20.0                # time until the ring decays below settle_frac of overshoot_m_s [s]
    peak_v_reelout_m_s: 1.16        # raw v_reelout max within ring_span [m/s]
    steady_v_reelout_m_s: 0.68      # mean v_reelout after ring_span [m/s]
traj_opt:
  power:
    predicted_W: 60                 # predicted mean reel-out power of the paths actually flown, weighted by their share of the reeling window [W]
    predicted_initial_W: -113       # predicted mean reel-out power of the path installed before the run [W]
    predicted_paths:
      t_000.0_s: -113               # 22% of the reeling window
      t_140.0_s: -301               # 8% of the reeling window
      t_205.5_s: -28                # 10% of the reeling window
      t_261.1_s: 103                # 10% of the reeling window
      t_314.1_s: 152                # 11% of the reeling window
      t_366.5_s: 213                # 13% of the reeling window
      t_423.8_s: 219                # 26% of the reeling window
    measured_W: 414                 # mean reel-out power the run harvested [W]
    ratio: 6.94                     # measured / predicted, against the weighted prediction
  guess:
    a_deg: 30.0                     # width of the guess lemniscate; azimuth spans ±a [deg]
    b_deg: 12.0                     # height of the guess, peak to peak [deg]
    el_center_deg: 29.0             # centre elevation of the guess [deg]
    points: 361                     # points the guess was sent with
    depower_seed_m: 1.6             # power-tape length the solve started from, input_depower ramped with the wind above input_depower_wind_ref; only a seed, the server optimizes it [m]
    depower_optimized:
      t_000.0_s: 1.56               # optimizer's l_dp [m]; rel_depower equivalent 0.291 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_140.0_s: 1.563              # optimizer's l_dp [m]; rel_depower equivalent 0.292 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_205.5_s: 1.556              # optimizer's l_dp [m]; rel_depower equivalent 0.290 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_261.1_s: 1.554              # optimizer's l_dp [m]; rel_depower equivalent 0.290 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_314.1_s: 1.557              # optimizer's l_dp [m]; rel_depower equivalent 0.290 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_366.5_s: 1.558              # optimizer's l_dp [m]; rel_depower equivalent 0.291 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_423.8_s: 1.556              # optimizer's l_dp [m]; rel_depower equivalent 0.290 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
    k_v_optimized:
  path:
    points: 360                     # points of the path installed before the run
    points_final: 360               # points of the path at the end; differs when reopt installed one
    az_center_deg: -1.6             # centre azimuth of the path [deg]
    az_amplitude_deg: 32.4          # half-width of the STARTUP path; the criteria are scored against the pattern commanded at each lap, whose mean is lift_budget's reference [deg]
    el_center_deg: 30.3             # centre elevation of the path [deg]
    el_height_deg: 21.3             # elevation span of the path, peak to peak [deg]
    downloops: true                 # traversal direction the optimizer solved for
  feasibility:
    min_required: 0.82              # min_feasibility_margin of data/traj_opt.yaml [-]
    turn_radius_headroom: 1.25      # factor on the gate's number for what the GATE adds: its finite-difference curvature estimate and the elevation lift [-]
    turn_radius_request_m: 12.19    # minimum turn radius asked of the optimizer for the LAST request; the gate's margin/(c1*max_steering) times the headroom and the lap's reel-out ratio, since the optimizer measures the radius up the reel-out while the run flies the curve at the anchor [m]
    turn_radius_scale: 1.304        # the two corrections together, as sent [-]
    turn_radius_lap_reelout_m: 0.0  # reel-out per lap ASSUMED for the startup request, the only one with no reply to measure it off [m]
    margin_start: 0.95              # curvature margin at the starting length; the worst case only when one path is flown throughout — see the docstring [-]
    margin_end: 2.4                 # curvature margin at reelout_l_max [-]
    c1_final: 0.1919                # turn-rate gain at depower_final [1/m]
    margin_final: 1.63              # curvature margin phase 5 flies with: depower_final's c1, at reelout_l_max, on the STARTING path lifted by el_offset_final [-]
    margin_final_flown: 0.95        # the same, for the last path the re-optimizer installed — the one phase 5 actually inherited [-]
  reopt:
    enabled: true                   # re-optimization during the run
    warm_start: true                # /step alone, seeded from the previous optimum; false re-inits from the parametric guess at every length
    requests: 7                     # solves that completed, accepted or rejected
    blocking: true                  # simulation held while a solve ran
    blocked_s: 42.2                 # wall time the simulation was frozen waiting for replies [s]
    installed: 6                    # new paths actually flown
    blend_retries: 0                # cold-restart attempts spent on a rejected reply — a folded blend, a collapsed prediction, or a clearance/elevation shortfall re-asked at a raised floor — across every install this run
    events:
      t_080.6_s: "skipped — cached failure from 2026-08-29 00:24:51 (solver failed at L = 101.6 m, guess el 29°)" # at L = 102 m
      t_080.6_s_2: "failed"         # at L = 102 m
      t_140.0_s: "installed — margin 1.09 (phase 5: 2.09 at 380 m), clearance 54.9 m, -301 W predicted" # at L = 137 m
      t_205.5_s: "installed — margin 0.94 (phase 5: 1.55 at 380 m), clearance 53.1 m, -28 W predicted" # at L = 160 m
      t_261.1_s: "installed — margin 0.92 (phase 5: 1.26 at 380 m), clearance 53.0 m, 103 W predicted" # at L = 192 m
      t_314.1_s: "installed — margin 0.98 (phase 5: 1.15 at 380 m), clearance 53.3 m, 152 W predicted" # at L = 225 m
      t_366.5_s: "installed — margin 1.05 (phase 5: 1.07 at 380 m), clearance 56.4 m, 213 W predicted" # at L = 259 m
      t_423.8_s: "installed — margin 1.07 (phase 5: 0.95 at 380 m), clearance 68.8 m, 219 W predicted" # at L = 299 m
  el_bias:
    gain: 0.5                       # per-lap learning gain for the elevation bias; 0 = off
    final_deg: 1.08                 # mean elevation correction added to the last installed path [deg]
    bins: 5                         # el_bias_bins, azimuth bands the correction is learnt over; 1 = one rigid shift
    smooth: 0.25                    # el_bias_smooth, how far each band is pulled towards its neighbours after every lap [-]
    profile_deg: [0.68, 0.85, 1.03, 1.5, 1.36] # the learnt correction per band, crossing first [deg]
    lift_deg: 1.5                   # el_offset_final, the fixed lift added on top of the learnt bias [deg]
    lift_lead_s: 8.0                # el_offset_lead, how early the lift is allowed to latch; 0 = at the end [s]
    wing_deg: 1.5                   # el_offset_wing, extra lift at the lobes, baked into every installed path [deg]
    wing_mode: "azimuth"            # el_offset_wing_mode, the coordinate that lift is shaped over
    wing_az_deg: 10.0               # azimuth mode: azimuth beyond which that lift is full; it ramps over el_offset_wing_blend below it [deg]
    wing_depth: 0.0                 # elevation mode: depth below the path's elevation centre where the lift starts, in half-spans; full at its lowest point [-]
    lift_t_s: 533.2                 # when the lift actually latched [s]
    lift_remaining_m: 6.9           # reel-out left at that moment; > 0 means the lead fired, 0 means phase 5 did [m]
    shift_delivery:
      t_080.6_s: "held back"        # +1.00° of shift (mean over the bands), curvature margin 0.67 vs 0.82 required
      t_104.7_s: "blended in (100 % of the mean, 50 % of the spread)" # +1.00° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_108.7_s: "blended in (100 % of the mean, 50 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.84 vs 0.82 required
      t_112.7_s: "blended in (100 % of the mean, 75 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.83 vs 0.82 required
      t_116.7_s: "blended in (100 % of the mean, 50 % of the spread)" # -0.00° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_120.7_s: "held back"        # +0.00° of shift (mean over the bands), curvature margin 0.81 vs 0.82 required
      t_124.0_s: "blended in (100 % of the mean, 75 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_128.0_s: "blended in (100 % of the mean, 25 % of the spread)" # +0.33° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_132.0_s: "blended in (100 % of the mean, 50 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_136.0_s: "blended in (100 % of the mean, 75 % of the spread)" # -0.00° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_140.0_s: "carried by an install" # +0.00° of shift (mean over the bands), curvature margin 1.09 vs 0.82 required
      t_187.6_s: "blended in"       # -0.02° of shift (mean over the bands), curvature margin 1.18 vs 0.82 required
      t_244.4_s: "blended in"       # -0.24° of shift (mean over the bands), curvature margin 1.16 vs 0.82 required
      t_298.3_s: "blended in"       # -0.19° of shift (mean over the bands), curvature margin 1.15 vs 0.82 required
      t_350.7_s: "blended in"       # -0.08° of shift (mean over the bands), curvature margin 1.05 vs 0.82 required
      t_404.4_s: "blended in"       # +0.11° of shift (mean over the bands), curvature margin 1.15 vs 0.82 required
      t_467.7_s: "blended in"       # +0.17° of shift (mean over the bands), curvature margin 1.18 vs 0.82 required
      t_533.2_s: "blended in"       # +1.50° of shift (mean over the bands), curvature margin 1.30 vs 0.82 required
    laps:
      lap_1: -1.73                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.73°, correction became [+0.70 +0.81 +0.74 +1.58 +1.17]° (crossing … lobe)
      lap_2: -0.5                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.40°, correction became [+1.00 +1.12 +1.00 +2.01 +1.53]° (crossing … lobe)
      lap_3: 0.07                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.13°, correction became [+0.98 +1.00 +1.12 +1.94 +1.50]° (crossing … lobe)
      lap_4: 0.48                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.74°, correction became [+0.91 +0.96 +0.82 +1.40 +1.23]° (crossing … lobe)
      lap_5: 0.39                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.64°, correction became [+0.83 +0.99 +0.70 +0.93 +0.95]° (crossing … lobe)
      lap_6: 0.24                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.60°, correction became [+0.72 +0.92 +0.61 +0.91 +0.82]° (crossing … lobe)
      lap_7: -0.2                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.98°, correction became [+0.67 +0.86 +0.77 +1.20 +1.06]° (crossing … lobe)
      lap_8: -0.34                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.22°, correction became [+0.68 +0.85 +1.03 +1.50 +1.36]° (crossing … lobe)
    lap_profiles:
      lap_1: "-1.33 -1.82 -1.11 -3.78 -1.94" # error vs the optimizer's curve per band, crossing first; 466/752/1288/398/544 samples, correction now [+0.70 +0.81 +0.74 +1.58 +1.17]° (crossing … lobe)
      lap_2: "-0.52 -0.89 -0.07 -1.60 -0.24" # error vs the optimizer's curve per band, crossing first; 616/890/1576/432/601 samples, correction now [+1.00 +1.12 +1.00 +2.01 +1.53]° (crossing … lobe)
      lap_3: "+0.02 +0.18 +0.02 -0.47 +0.48" # error vs the optimizer's curve per band, crossing first; 921/1546/1826/535/688 samples, correction now [+0.98 +1.00 +1.12 +1.94 +1.50]° (crossing … lobe)
      lap_4: "+0.17 -0.08 +0.88 +0.72 +0.73" # error vs the optimizer's curve per band, crossing first; 939/1232/1722/558/659 samples, correction now [+0.91 +0.96 +0.82 +1.40 +1.23]° (crossing … lobe)
      lap_5: "+0.34 -0.32 +0.47 +0.80 +0.58" # error vs the optimizer's curve per band, crossing first; 854/895/1415/1055/634 samples, correction now [+0.83 +0.99 +0.70 +0.93 +0.95]° (crossing … lobe)
      lap_6: "+0.39 -0.13 +0.42 -0.19 +0.33" # error vs the optimizer's curve per band, crossing first; 842/912/1881/419/660 samples, correction now [+0.72 +0.92 +0.61 +0.91 +0.82]° (crossing … lobe)
      lap_7: "+0.25 -0.08 -0.11 -0.88 -0.32" # error vs the optimizer's curve per band, crossing first; 915/973/1440/869/635 samples, correction now [+0.67 +0.86 +0.77 +1.20 +1.06]° (crossing … lobe)
      lap_8: "+0.06 -0.05 -0.42 -0.90 -0.44" # error vs the optimizer's curve per band, crossing first; 1105/1262/1523/1088/716 samples, correction now [+0.68 +0.85 +1.03 +1.50 +1.36]° (crossing … lobe)
  droop_profile:
    az_00_20_pct: 7.54              # kite below the path's elevation centre [deg], over 9145 samples; the path itself sits 0.81 half-spans down there and the kite -0.90° under it
    az_20_40_pct: 6.15              # kite below the path's elevation centre [deg], over 10244 samples; the path itself sits 0.59 half-spans down there and the kite -1.20° under it
    az_40_60_pct: 1.98              # kite below the path's elevation centre [deg], over 14464 samples; the path itself sits 0.16 half-spans down there and the kite -0.87° under it
    az_60_80_pct: 1.35              # kite below the path's elevation centre [deg], over 6689 samples; the path itself sits -0.05 half-spans down there and the kite -1.89° under it
    az_80_100_pct: -0.16            # kite below the path's elevation centre [deg], over 6036 samples; the path itself sits -0.12 half-spans down there and the kite -1.22° under it
    centre_to_lobe_deg: 0.0         # how much deeper the kite flies in its worst azimuth band than in the crossing — what a SHAPED lift is aimed at, where el_bias and el_offset_final can only move the pattern rigidly [deg]
  lift_budget:
    delivered_deg: 2.58             # mean correction the path in the air actually carries — el_bias plus el_offset_final once it has been delivered [deg]
    delivered_profile_deg: [2.18, 2.35, 2.53, 3.0, 2.86] # the same, per azimuth band, crossing first [deg]
    commanded_az_amp_deg: 27.71     # mean half-width the run was COMMANDED to fly, against the startup path's in traj_opt.path [deg]
    commanded_el_height_deg: 16.9   # mean commanded pattern height [deg]
    wing_deg: 1.5                   # el_offset_wing, the fixed lobe lift baked into every installed path [deg]
    el_min_run_deg: 11.8            # lowest elevation over the whole run — set in phase 4, so a phase-5 lift does not move it [deg]
    el_min_final_deg: 13.19         # lowest elevation in phase 5, which is what el_offset_final buys [deg]
    azimuth_reach_pos_deg: 7.22     # reach margin: flew 26.61° against the 19.40° required by min_span_frac = 0.70, +37 %
    azimuth_reach_neg_deg: 7.77     # reach margin: flew 27.17° against the 19.40° required by min_span_frac = 0.70, +40 %
    elevation_span_deg: 1.15        # reach margin: flew 12.98° against the 11.83° required by min_span_frac = 0.70, +10 %
    tightest: "elevation span"      # the size criterion with the least room, +1.15° (+10 %) — what the NEXT lift has to spend
  clearance:
    path_min_m: 50.4                # lowest point of the PRE-FLIGHT path at the starting length [m]
    flown_min_m: 22.5               # lowest point the kite actually reached over the run [m]
    min_required_m: 40.0            # min_height of data/traj_opt.yaml [m]
    elevation_min_asked_deg: 9.5    # elevation floor the LAST request carried, the higher of asind(min_height/L) and min_elevation + candidate_elevation_margin at the length it was made for; the optimizer constrains HEIGHT, and only at the far end of the lap's reel-out [deg]
    elevation_min_extra_deg: 0.0    # raise a rejected reply's shortfall added to that floor, carried forward once measured; 0 means no reply was gated out low [deg]
  inflow:
    wind_speed_m_s: 3.0             # wind speed at 6 m sent to the optimizer [m/s]
    wind_direction_deg: 270.0       # direction the wind comes from [deg]
    profile_law: 3                  # 0=CONST, 1=EXP, 2=LOG, 3=EXPLOG, 4-6=CUSTOM_*
  winch:
    k_v: 0.0408                     # v_set = k_v * sqrt(force) sent to the optimizer [-]
    optimize_k_v: false             # k_v sent as a design variable; the reply's value is then the one flown
    k_v_flown: 0.0408               # k_v the run actually flew; == k_v, the optimizer did not move it
    f_min_N: 350.0                  # minimum winch force sent [N]
    f_max_N: 8000.0                 # maximum winch force sent [N]
    force_limit: "soft"             # the reel-out law itself limits the force, inverting the optimizer's own saturating tension curve (beta 3e-02/1e-03, force filtered at tau = 2.48 s); the UpperForceController is held in reset, so upper_force_pct is 0 by construction
performance:
  sim_time_s: 559.8                 # simulated time [s]
  wall_time_s: 182.7                # wall-clock time [s]
  total_wall_time_s: 303.3          # the whole script: package loading, init, settling, the startup solve and the run, up to this summary — the archive copy and any plots follow it [s]
  optimization_time_s: 52.0         # wall time waiting for the optimizer: the startup solve (9.8 s) plus every blocking re-optimization (traj_opt.reopt.blocked_s) [s]
  realtime_factor: 3.98             # sim_time / wall_time, excluding time frozen for re-optimization
  ms_per_step: 2.8                  # wall time per step, excluding time frozen for re-optimization [ms]
  steps: 50384                      # step count
  dt_s: 0.011111111111111112        # simulation timestep [s]
  vsm_interval: 10                  # VSM aerodynamic update interval [steps]
summary:
  date: "2026-08-29"                # wall-clock date the run finished
  time: "00:34:33"                  # wall-clock time the run finished
  wind_speed_gnd: 3.0               # wind speed at 6 m sent to the optimizer [m/s]
  power_ratio: 6.94                 # measured / predicted reel-out power [-]
  success_criteria: "all 10 passed" # pass/fail verdict vs V3Kite's success criteria
  total_wall_time_s: 303.3          # the whole script, start to this summary [s]
  realtime_factor: 3.98             # sim_time / wall_time, excluding time frozen for re-optimization
  optimization_requests: 7          # solves that completed, accepted or rejected
  optimizations_installed: 6        # new paths actually flown
  av_power_ro: 364                  # mean reel-out power over phase four [W]
  min_power_ro: -81                 # min reel-out power over phase four [W]
  max_power_ro: 1158                # max reel-out power over phase four [W]
  min_force_ro: 272                 # min tether force over phase four [N]
  av_force_ro: 510                  # mean tether force over phase four [N]
  max_force_ro: 1106                # max tether force over phase four [N]
  v_ro_min: -0.24                   # min reel-out speed over phase four [m/s]
  v_ro_av: 0.6                      # mean reel-out speed over phase four [m/s]
  v_ro_max: 1.19                    # max reel-out speed over phase four [m/s]
  av_depower_ro: 0.291              # mean KCU depower over phase four [-]
