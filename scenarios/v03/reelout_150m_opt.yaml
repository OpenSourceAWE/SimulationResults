# Run summary for output/reelout_150m_opt.arrow, written by examples/simple_reelout.jl at the end of the run.
success_criteria: "all 9 passed"    # pass/fail verdict vs V3Kite's success criteria
simulation:
  script: "simple_opt_reelout.jl"   # script that produced this run
  project: "system_reelout_150m.yaml" # system project flown
  rel_turbulence: "default"         # turbulence level in [0, 1] passed to init
  wind_speed: 3.0                   # mean wind speed in m/s passed to init
  time: "11:55:12"                  # wall-clock time the run finished
  date: "2026-08-24"                # wall-clock date the run finished
  hostname: "ufryzen"               # machine the run executed on
  git_hash: "f398bb8"               # SimpleKiteControllers.jl commit hash
  git_status: "dirty"               # SimpleKiteControllers.jl working tree: clean or dirty
fig8_metrics:
  settled_from_s: 41.5              # sim time the scoring window begins [s]
  settle_time_s: 39.5               # time after t_start to converge [s]
  laps: 8.5                         # figure-eight laps completed
  cross_track_deg:
    rms: 1.95                       # RMS cross-track error [deg]
    mean: 1.66                      # mean cross-track error [deg]
    max: 5.54                       # max cross-track error [deg]
  elevation_deg:
    min_settled: 10.8               # min elevation, settled window [deg]
    min_whole_run: 10.8             # min elevation, whole run [deg]
  peak_turn_rate_deg_s: 58          # peak heading rate [deg/s]
  extent:
    azimuth_deg:
      min: -26.0                    # flown azimuth reach, negative side [deg]
      max: 25.7                     # flown azimuth reach, positive side [deg]
    azimuth_pct_of_amplitude:
      min: 98                       # negative reach as % of the pattern's ±A
      max: 99                       # positive reach as % of the pattern's ±A
    worst_lobe_deg:
      min: -18.8                    # weakest lobe, negative side [deg]
      max: 18.6                     # weakest lobe, positive side [deg]
    elevation_span_deg: 26.0        # flown elevation span over the whole settled window — the pattern DESCENDS as the tether grows, so this is mostly that descent [deg]
    elevation_span_lap_deg: 11.1    # flown elevation span per lobe, which is the pattern's own height [deg]
    elevation_span_pct_of_b: 82     # flown span as % of the B commanded where it was flown
  tether_force_N:
    mean: 587                       # mean tether force, settled window [N]
    std: 165                        # std tether force, settled window [N]
    cv_pct: 28.0                    # coefficient of variation [%]
  steering:
    peak_abs_u_s: 0.32              # peak |rel_steering| commanded
    pct_time_within_2pct_of_peak: 10 # % of time within 2% of peak (saturation)
    hf_std_steering: 0.0114         # high-frequency std of steering (chatter)
    hf_std_turnrate_deg_s: 0.14     # high-frequency std of heading rate [deg/s]
  tape:
    delivered_peak_abs_u_s: 0.32    # peak steering the KCU tape delivered
    commanded_peak_abs_u_s: 0.32    # peak steering commanded
    rate_limited_pct_time: 4        # % of time the tape's rate limit was hit
    rate_limited_peak_per_s: 0.2    # peak tape rate reached [1/s]
    rate_limit_per_s: 0.2           # KCU's configured rate limit [1/s]
reelout:
  v_app_phase4:
    duration_s: 330.0               # phase-4 window length [s]
    mean_m_s: 9.13                  # mean apparent wind speed [m/s]
    min_m_s: 4.94                   # min apparent wind speed [m/s]
    max_m_s: 14.88                  # max apparent wind speed [m/s]
    v_app_ref_m_s: 27.0             # reference apparent wind speed [m/s]
    deviation_pct: -66.2            # mean v_app deviation from v_app_ref [%]
  tether:
    start_m: 150.0                  # tether length at run start [m]
    end_m: 380.0                    # tether length at run end [m]
    target_m: 380.0                 # reelout_l_max target [m]
    stop_reason: "length"           # criterion that ended reel-out: length, laps, or none
    laps_reeled: 8.34               # figure-eight laps completed by the time reel-out ended
  force:
    cf_force_ro: 1.72               # crest factor: peak / mean tether force over the reeling window
  power:
    cf_power_ro: 2.31               # crest factor: peak / mean reel-out power over the reeling window
    duration_s: 334.4               # reeling window length [s]
    n_samples: 30100                # sample count in the reeling window
    energy_kJ: 187.1                # energy over the reeling window [kJ]
    energy_run_kJ: 163.5            # energy over the whole run [kJ]
  winch_state:
    speed_pct: 79.4                 # % of the reeling window in speed control (state 1)
    lower_force_pct: 20.6           # % in the LowerForceController, reeling in (state 0)
    upper_force_pct: 0.0            # % in the UpperForceController, force capped at f_high (state 2)
    n_samples: 30100                # sample count in the reeling window
  ringing:
    n_peaks: 3                      # ring peaks detected above peak_floor
    period_s: 8.02                  # mean peak-to-peak ring period [s]
    zeta: 0.064                     # damping ratio from the peak log decrement
    overshoot_m_s: 0.1              # first ring peak's amplitude above the local trend [m/s]
    duration_s: 20.0                # time until the ring decays below settle_frac of overshoot_m_s [s]
    peak_v_reelout_m_s: 1.12        # raw v_reelout max within ring_span [m/s]
    steady_v_reelout_m_s: 0.9       # mean v_reelout after ring_span [m/s]
traj_opt:
  power:
    predicted_W: 388                # predicted mean reel-out power of the paths actually flown, weighted by their share of the reeling window [W]
    predicted_initial_W: 205        # predicted mean reel-out power of the path installed before the run [W]
    predicted_paths:
      t_000.0_s: 205                # 27% of the reeling window
      t_128.1_s: 202                # 17% of the reeling window
      t_186.1_s: 412                # 16% of the reeling window
      t_240.1_s: 543                # 16% of the reeling window
      t_292.1_s: 607                # 16% of the reeling window
      t_345.0_s: 639                # 8% of the reeling window
    measured_W: 559                 # mean reel-out power the run harvested [W]
    ratio: 1.44                     # measured / predicted, against the weighted prediction
  guess:
    a_deg: 30.0                     # width of the guess lemniscate; azimuth spans ±a [deg]
    b_deg: 12.0                     # height of the guess, peak to peak [deg]
    el_center_deg: 29.0             # centre elevation of the guess [deg]
    points: 361                     # points the guess was sent with
    depower_seed_m: 1.6             # power-tape length the solve started from, input_depower ramped with the wind above input_depower_wind_ref; only a seed, the server optimizes it [m]
    depower_optimized:
      t_000.0_s: 1.557              # optimizer's l_dp [m]; rel_depower equivalent 0.296 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_128.1_s: 1.556              # optimizer's l_dp [m]; rel_depower equivalent 0.296 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_186.1_s: 1.544              # optimizer's l_dp [m]; rel_depower equivalent 0.294 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_240.1_s: 1.53               # optimizer's l_dp [m]; rel_depower equivalent 0.291 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_292.1_s: 1.522              # optimizer's l_dp [m]; rel_depower equivalent 0.289 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_345.0_s: 1.516              # optimizer's l_dp [m]; rel_depower equivalent 0.288 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
  path:
    points: 360                     # points of the path installed before the run
    points_final: 360               # points of the path at the end; differs when reopt installed one
    az_center_deg: 0.2              # centre azimuth of the path [deg]
    az_amplitude_deg: 31.5          # half-width of the STARTUP path; the criteria are scored against the pattern commanded at each lap, whose mean is lift_budget's reference [deg]
    el_center_deg: 29.2             # centre elevation of the path [deg]
    el_height_deg: 19.6             # elevation span of the path, peak to peak [deg]
    downloops: true                 # traversal direction the optimizer solved for
  feasibility:
    min_required: 0.82              # min_feasibility_margin of data/traj_opt.yaml [-]
    turn_radius_headroom: 1.2       # factor on the gate's number for what the GATE adds: its finite-difference curvature estimate and the elevation lift [-]
    turn_radius_request_m: 12.17    # minimum turn radius asked of the optimizer for the LAST request; the gate's margin/(c1*max_steering) times the headroom and the lap's reel-out ratio, since the optimizer measures the radius up the reel-out while the run flies the curve at the anchor [m]
    turn_radius_scale: 1.308        # the two corrections together, as sent [-]
    turn_radius_lap_reelout_m: 0.0  # reel-out per lap ASSUMED for the startup request, the only one with no reply to measure it off [m]
    margin_start: 0.93              # curvature margin at the starting length; the worst case only when one path is flown throughout — see the docstring [-]
    margin_end: 2.36                # curvature margin at reelout_l_max [-]
    c1_final: 0.1919                # turn-rate gain at depower_final [1/m]
    margin_final: 1.61              # curvature margin phase 5 flies with: depower_final's c1, at reelout_l_max, on the STARTING path lifted by el_offset_final [-]
    margin_final_flown: 0.8         # the same, for the last path the re-optimizer installed — the one phase 5 actually inherited [-]
  reopt:
    enabled: true                   # re-optimization during the run
    warm_start: true                # /step alone, seeded from the previous optimum; false re-inits from the parametric guess at every length
    requests: 6                     # solves that completed, accepted or rejected
    blocking: true                  # simulation held while a solve ran
    blocked_s: 96.4                 # wall time the simulation was frozen waiting for replies [s]
    installed: 5                    # new paths actually flown
    blend_retries: 0                # cold-restart attempts spent on a rejected reply — a folded blend, a collapsed prediction, or a clearance/elevation shortfall re-asked at a raised floor — across every install this run
    events:
      t_079.7_s: "failed"           # at L = 114 m
      t_128.1_s: "installed — margin 0.98 (phase 5: 1.73 at 380 m), clearance 51.4 m, 202 W predicted" # at L = 149 m
      t_186.1_s: "installed — margin 0.97 (phase 5: 1.34 at 380 m), clearance 54.5 m, 412 W predicted" # at L = 190 m
      t_240.1_s: "installed — margin 1.00 (phase 5: 1.09 at 380 m), clearance 55.2 m, 543 W predicted" # at L = 243 m
      t_292.1_s: "installed — margin 1.01 (phase 5: 0.90 at 380 m), clearance 62.4 m, 607 W predicted" # at L = 298 m
      t_345.0_s: "installed — margin 1.08 (phase 5: 0.80 at 380 m), clearance 77.8 m, 639 W predicted" # at L = 354 m
  el_bias:
    gain: 0.5                       # per-lap learning gain for the elevation bias; 0 = off
    final_deg: 2.2                  # mean elevation correction added to the last installed path [deg]
    bins: 5                         # el_bias_bins, azimuth bands the correction is learnt over; 1 = one rigid shift
    smooth: 0.25                    # el_bias_smooth, how far each band is pulled towards its neighbours after every lap [-]
    profile_deg: [1.21, 1.64, 2.39, 3.11, 2.64] # the learnt correction per band, crossing first [deg]
    lift_deg: 1.5                   # el_offset_final, the fixed lift added on top of the learnt bias [deg]
    lift_lead_s: 8.0                # el_offset_lead, how early the lift is allowed to latch; 0 = at the end [s]
    wing_deg: 1.5                   # el_offset_wing, extra lift at the lobes, baked into every installed path [deg]
    wing_mode: "azimuth"            # el_offset_wing_mode, the coordinate that lift is shaped over
    wing_az_deg: 10.0               # azimuth mode: azimuth beyond which that lift is full; it ramps over el_offset_wing_blend below it [deg]
    wing_depth: 0.0                 # elevation mode: depth below the path's elevation centre where the lift starts, in half-spans; full at its lowest point [-]
    lift_t_s: 361.3                 # when the lift actually latched [s]
    lift_remaining_m: 7.9           # reel-out left at that moment; > 0 means the lead fired, 0 means phase 5 did [m]
    shift_delivery:
      t_079.7_s: "blended in (100 % of the mean, 0 % of the spread)" # +0.72° of shift (mean over the bands), curvature margin 0.82 vs 0.82 required
      t_083.8_s: "blended in (100 % of the mean, 75 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.83 vs 0.82 required
      t_087.8_s: "blended in"       # +0.00° of shift (mean over the bands), curvature margin 0.84 vs 0.82 required
      t_132.1_s: "blended in"       # +0.28° of shift (mean over the bands), curvature margin 0.97 vs 0.82 required
      t_190.2_s: "blended in"       # -0.05° of shift (mean over the bands), curvature margin 1.00 vs 0.82 required
      t_244.1_s: "blended in"       # +0.06° of shift (mean over the bands), curvature margin 0.98 vs 0.82 required
      t_296.1_s: "blended in"       # +0.03° of shift (mean over the bands), curvature margin 1.05 vs 0.82 required
      t_349.1_s: "blended in"       # +0.10° of shift (mean over the bands), curvature margin 1.10 vs 0.82 required
      t_361.3_s: "blended in"       # +1.50° of shift (mean over the bands), curvature margin 1.12 vs 0.82 required
      t_452.9_s: "held back"        # +1.08° of shift (mean over the bands), curvature margin 0.79 vs 0.82 required
    laps:
      lap_1: -1.4                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.40°, correction became [+0.58 +0.73 +0.65 +0.83 +0.80]° (crossing … lobe)
      lap_2: -0.52                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.23°, correction became [+0.78 +0.99 +0.89 +1.18 +1.14]° (crossing … lobe)
      lap_3: 0.13                   # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.85°, correction became [+0.78 +1.02 +0.85 +1.00 +1.05]° (crossing … lobe)
      lap_4: -0.14                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.08°, correction became [+0.67 +0.96 +1.02 +1.26 +1.08]° (crossing … lobe)
      lap_5: -0.05                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.05°, correction became [+0.57 +0.89 +1.11 +1.36 +1.21]° (crossing … lobe)
      lap_6: -0.17                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.17°, correction became [+0.50 +0.86 +1.23 +1.58 +1.44]° (crossing … lobe)
      lap_8: -1.05                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -2.11°, correction became [+1.21 +1.64 +2.39 +3.11 +2.64]° (crossing … lobe)
    lap_profiles:
      lap_1: "-1.04 -1.60 -1.19 -1.79 -1.57" # error vs the optimizer's curve per band, crossing first; 487/563/1236/621/535 samples, correction now [+0.58 +0.73 +0.65 +0.83 +0.80]° (crossing … lobe)
      lap_2: "-0.23 -0.71 -0.32 -0.88 -0.62" # error vs the optimizer's curve per band, crossing first; 635/693/1525/793/706 samples, correction now [+0.78 +0.99 +0.89 +1.18 +1.14]° (crossing … lobe)
      lap_3: "+0.20 -0.29 +0.20 +0.27 +0.15" # error vs the optimizer's curve per band, crossing first; 781/837/1692/1178/737 samples, correction now [+0.78 +1.02 +0.85 +1.00 +1.05]° (crossing … lobe)
      lap_4: "+0.43 -0.06 -0.26 -0.74 +0.06" # error vs the optimizer's curve per band, crossing first; 918/931/1108/1077/822 samples, correction now [+0.67 +0.96 +1.02 +1.26 +1.08]° (crossing … lobe)
      lap_5: "+0.40 +0.02 -0.16 -0.41 -0.15" # error vs the optimizer's curve per band, crossing first; 972/970/1028/989/718 samples, correction now [+0.57 +0.89 +1.11 +1.36 +1.21]° (crossing … lobe)
      lap_6: "+0.34 -0.03 -0.25 -0.69 -0.32" # error vs the optimizer's curve per band, crossing first; 1069/1048/1032/984/635 samples, correction now [+0.50 +0.86 +1.23 +1.58 +1.44]° (crossing … lobe)
      lap_8: "-0.60 -0.80 -1.17 -1.80 -1.00" # error vs the optimizer's curve per band, crossing first; 1121/1155/967/911/625 samples, correction now [+1.21 +1.64 +2.39 +3.11 +2.64]° (crossing … lobe)
  droop_profile:
    az_00_20_pct: 5.6               # kite below the path's elevation centre [deg], over 7641 samples; the path itself sits 0.83 half-spans down there and the kite -0.67° under it
    az_20_40_pct: 4.54              # kite below the path's elevation centre [deg], over 8016 samples; the path itself sits 0.52 half-spans down there and the kite -1.29° under it
    az_40_60_pct: 2.64              # kite below the path's elevation centre [deg], over 10004 samples; the path itself sits 0.16 half-spans down there and the kite -1.38° under it
    az_60_80_pct: 1.2               # kite below the path's elevation centre [deg], over 7750 samples; the path itself sits -0.09 half-spans down there and the kite -2.02° under it
    az_80_100_pct: 1.16             # kite below the path's elevation centre [deg], over 5517 samples; the path itself sits 0.01 half-spans down there and the kite -1.56° under it
    centre_to_lobe_deg: 0.0         # how much deeper the kite flies in its worst azimuth band than in the crossing — what a SHAPED lift is aimed at, where el_bias and el_offset_final can only move the pattern rigidly [deg]
  lift_budget:
    delivered_deg: 2.62             # mean correction the path in the air actually carries — el_bias plus el_offset_final once it has been delivered [deg]
    delivered_profile_deg: [2.0, 2.36, 2.73, 3.08, 2.94] # the same, per azimuth band, crossing first [deg]
    commanded_az_amp_deg: 25.9      # mean half-width the run was COMMANDED to fly, against the startup path's in traj_opt.path [deg]
    commanded_el_height_deg: 13.4   # mean commanded pattern height [deg]
    wing_deg: 1.5                   # el_offset_wing, the fixed lobe lift baked into every installed path [deg]
    el_min_run_deg: 10.8            # lowest elevation over the whole run — set in phase 4, so a phase-5 lift does not move it [deg]
    el_min_final_deg: 10.82         # lowest elevation in phase 5, which is what el_offset_final buys [deg]
    azimuth_reach_pos_deg: 7.46     # reach margin: flew 25.59° against the 18.13° required by min_span_frac = 0.70, +41 %
    azimuth_reach_neg_deg: 7.22     # reach margin: flew 25.35° against the 18.13° required by min_span_frac = 0.70, +40 %
    elevation_span_deg: 1.65        # reach margin: flew 11.03° against the 9.38° required by min_span_frac = 0.70, +18 %
    tightest: "elevation span"      # the size criterion with the least room, +1.65° (+18 %) — what the NEXT lift has to spend
  clearance:
    path_min_m: 49.8                # lowest point of the PRE-FLIGHT path at the starting length [m]
    flown_min_m: 29.0               # lowest point the kite actually reached over the run [m]
    min_required_m: 40.0            # min_height of data/traj_opt.yaml [m]
    elevation_min_asked_deg: 9.5    # elevation floor the LAST request carried, the higher of asind(min_height/L) and min_elevation + candidate_elevation_margin at the length it was made for; the optimizer constrains HEIGHT, and only at the far end of the lap's reel-out [deg]
    elevation_min_extra_deg: 0.0    # raise a rejected reply's shortfall added to that floor, carried forward once measured; 0 means no reply was gated out low [deg]
  inflow:
    wind_speed_m_s: 3.0             # wind speed at 6 m sent to the optimizer [m/s]
    wind_direction_deg: 270.0       # direction the wind comes from [deg]
    profile_law: 3                  # 0=CONST, 1=EXP, 2=LOG, 3=EXPLOG, 4-6=CUSTOM_*
  winch:
    k_v: 0.0408                     # v_set = k_v * sqrt(force) sent to the optimizer [-]
    f_min_N: 350.0                  # minimum winch force sent [N]
    f_max_N: 8000.0                 # maximum winch force sent [N]
performance:
  sim_time_s: 474.0                 # simulated time [s]
  wall_time_s: 200.1                # wall-clock time [s]
  total_wall_time_s: 221.9          # the whole script: package loading, init, settling, the startup solve and the run, up to this summary — the archive copy and any plots follow it [s]
  optimization_time_s: 105.0        # wall time waiting for the optimizer: the startup solve (8.6 s) plus every blocking re-optimization (traj_opt.reopt.blocked_s) [s]
  realtime_factor: 4.57             # sim_time / wall_time, excluding time frozen for re-optimization
  ms_per_step: 2.4                  # wall time per step, excluding time frozen for re-optimization [ms]
  steps: 42662                      # step count
  dt_s: 0.011111111111111112        # simulation timestep [s]
  vsm_interval: 10                  # VSM aerodynamic update interval [steps]
summary:
  date: "2026-08-24"                # wall-clock date the run finished
  time: "11:55:12"                  # wall-clock time the run finished
  wind_speed_gnd: 3.0               # wind speed at 6 m sent to the optimizer [m/s]
  power_ratio: 1.44                 # measured / predicted mean reel-out power, against the weighted prediction
  total_wall_time_s: 221.9          # the whole script, start to this summary [s]
  realtime_factor: 4.57             # sim_time / wall_time, excluding time frozen for re-optimization
  optimization_requests: 6          # solves that completed, accepted or rejected
  optimizations_installed: 5        # new paths actually flown
  av_power_ro: 563                  # mean reel-out power over phase four [W]
  min_power_ro: -36                 # min reel-out power over phase four [W]
  max_power_ro: 1290                # max reel-out power over phase four [W]
  min_force_ro: 249                 # min tether force over phase four [N]
  av_force_ro: 585                  # mean tether force over phase four [N]
  max_force_ro: 1000                # max tether force over phase four [N]
  v_ro_min: -0.09                   # min reel-out speed over phase four [m/s]
  v_ro_av: 0.89                     # mean reel-out speed over phase four [m/s]
  v_ro_max: 1.29                    # max reel-out speed over phase four [m/s]
  av_depower_ro: 0.294              # mean KCU depower over phase four [-]
