# Run summary for output/reelout_150m_opt.arrow, written by examples/simple_reelout.jl at the end of the run.
success_criteria: "all 9 passed"    # pass/fail verdict vs V3Kite's success criteria
simulation:
  script: "simple_opt_reelout.jl"   # script that produced this run
  project: "system_reelout_150m.yaml" # system project flown
  rel_turbulence: "default"         # turbulence level in [0, 1] passed to init
  wind_speed: 7.0                   # mean wind speed in m/s passed to init
  time: "12:23:48"                  # wall-clock time the run finished
  date: "2026-08-26"                # wall-clock date the run finished
  hostname: "ufryzen"               # machine the run executed on
  git_hash: "59b6fab"               # SimpleKiteControllers.jl commit hash
  git_status: "dirty"               # SimpleKiteControllers.jl working tree: clean or dirty
fig8_metrics:
  settled_from_s: 29.0              # sim time the scoring window begins [s]
  settle_time_s: 27.0               # time after t_start to converge [s]
  laps: 8.0                         # figure-eight laps completed
  cross_track_deg:
    rms: 1.51                       # RMS cross-track error [deg]
    mean: 1.19                      # mean cross-track error [deg]
    max: 4.92                       # max cross-track error [deg]
  elevation_deg:
    min_settled: 9.7                # min elevation, settled window [deg]
    min_whole_run: 9.7              # min elevation, whole run [deg]
  peak_turn_rate_deg_s: 118         # peak heading rate [deg/s]
  extent:
    azimuth_deg:
      min: -15.3                    # flown azimuth reach, negative side [deg]
      max: 13.6                     # flown azimuth reach, positive side [deg]
    azimuth_pct_of_amplitude:
      min: 97                       # negative reach as % of the pattern's ±A
      max: 96                       # positive reach as % of the pattern's ±A
    worst_lobe_deg:
      min: -9.8                     # weakest lobe, negative side [deg]
      max: 9.8                      # weakest lobe, positive side [deg]
    elevation_span_deg: 20.3        # flown elevation span over the whole settled window — the pattern DESCENDS as the tether grows, so this is mostly that descent [deg]
    elevation_span_lap_deg: 7.7     # flown elevation span per lobe, which is the pattern's own height [deg]
    elevation_span_pct_of_b: 90     # flown span as % of the B commanded where it was flown
  tether_force_N:
    mean: 4806                      # mean tether force, settled window [N]
    std: 582                        # std tether force, settled window [N]
    cv_pct: 12.1                    # coefficient of variation [%]
  steering:
    peak_abs_u_s: 0.32              # peak |rel_steering| commanded
    pct_time_within_2pct_of_peak: 4 # % of time within 2% of peak (saturation)
    hf_std_steering: 0.0071         # high-frequency std of steering (chatter)
    hf_std_turnrate_deg_s: 0.76     # high-frequency std of heading rate [deg/s]
  tape:
    delivered_peak_abs_u_s: 0.304   # peak steering the KCU tape delivered
    commanded_peak_abs_u_s: 0.32    # peak steering commanded
    rate_limited_pct_time: 8        # % of time the tape's rate limit was hit
    rate_limited_peak_per_s: 0.2    # peak tape rate reached [1/s]
    rate_limit_per_s: 0.2           # KCU's configured rate limit [1/s]
reelout:
  v_app_phase4:
    duration_s: 79.6                # phase-4 window length [s]
    mean_m_s: 26.02                 # mean apparent wind speed [m/s]
    min_m_s: 24.06                  # min apparent wind speed [m/s]
    max_m_s: 31.54                  # max apparent wind speed [m/s]
    v_app_ref_m_s: 27.0             # reference apparent wind speed [m/s]
    deviation_pct: -3.6             # mean v_app deviation from v_app_ref [%]
  tether:
    start_m: 150.0                  # tether length at run start [m]
    end_m: 380.0                    # tether length at run end [m]
    target_m: 380.0                 # reelout_l_max target [m]
    stop_reason: "length"           # criterion that ended reel-out: length, laps, or none
    laps_reeled: 8.13               # figure-eight laps completed by the time reel-out ended
  force:
    cf_force_ro: 1.16               # crest factor: peak / mean tether force over the reeling window
  power:
    cf_power_ro: 1.13               # crest factor: peak / mean reel-out power over the reeling window
    duration_s: 82.5                # reeling window length [s]
    n_samples: 7428                 # sample count in the reeling window
    energy_kJ: 1167.1               # energy over the reeling window [kJ]
    energy_run_kJ: 1164.8           # energy over the whole run [kJ]
  winch_state:
    speed_pct: 100.0                # % of the reeling window in speed control (state 1)
    lower_force_pct: 0.0            # % in the LowerForceController, reeling in (state 0)
    upper_force_pct: 0.0            # % in the UpperForceController, force capped at f_high (state 2)
    n_samples: 7428                 # sample count in the reeling window
  ringing:
    n_peaks: 5                      # ring peaks detected above peak_floor
    period_s: 3.23                  # mean peak-to-peak ring period [s]
    zeta: 0.09                      # damping ratio from the peak log decrement
    overshoot_m_s: 0.3              # first ring peak's amplitude above the local trend [m/s]
    duration_s: 16.2                # time until the ring decays below settle_frac of overshoot_m_s [s]
    peak_v_reelout_m_s: 3.06        # raw v_reelout max within ring_span [m/s]
    steady_v_reelout_m_s: 2.87      # mean v_reelout after ring_span [m/s]
traj_opt:
  power:
    predicted_W: 14306              # predicted mean reel-out power of the paths actually flown, weighted by their share of the reeling window [W]
    predicted_initial_W: 12429      # predicted mean reel-out power of the path installed before the run [W]
    predicted_paths:
      t_000.0_s: 12429              # 19% of the reeling window
      t_041.8_s: 13796              # 14% of the reeling window
      t_053.6_s: 14459              # 14% of the reeling window
      t_065.3_s: 14823              # 14% of the reeling window
      t_077.0_s: 15058              # 15% of the reeling window
      t_089.2_s: 15227              # 19% of the reeling window
      t_105.3_s: 15329              # 4% of the reeling window
    measured_W: 14141               # mean reel-out power the run harvested [W]
    ratio: 0.99                     # measured / predicted, against the weighted prediction
  guess:
    a_deg: 30.0                     # width of the guess lemniscate; azimuth spans ±a [deg]
    b_deg: 12.0                     # height of the guess, peak to peak [deg]
    el_center_deg: 29.0             # centre elevation of the guess [deg]
    points: 361                     # points the guess was sent with
    depower_seed_m: 1.6             # power-tape length the solve started from, input_depower ramped with the wind above input_depower_wind_ref; only a seed, the server optimizes it [m]
    depower_optimized:
      t_000.0_s: 1.431              # optimizer's l_dp [m]; rel_depower equivalent 0.271 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_041.8_s: 1.424              # optimizer's l_dp [m]; rel_depower equivalent 0.270 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_053.6_s: 1.421              # optimizer's l_dp [m]; rel_depower equivalent 0.269 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_065.3_s: 1.419              # optimizer's l_dp [m]; rel_depower equivalent 0.269 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_077.0_s: 1.417              # optimizer's l_dp [m]; rel_depower equivalent 0.268 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_089.2_s: 1.416              # optimizer's l_dp [m]; rel_depower equivalent 0.268 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
      t_105.3_s: 1.416              # optimizer's l_dp [m]; rel_depower equivalent 0.268 (awetrim_depower_to_v3kite), against the flown depower_setpoint = 0.274
    k_v_optimized:
  path:
    points: 360                     # points of the path installed before the run
    points_final: 360               # points of the path at the end; differs when reopt installed one
    az_center_deg: 0.6              # centre azimuth of the path [deg]
    az_amplitude_deg: 21.1          # half-width of the STARTUP path; the criteria are scored against the pattern commanded at each lap, whose mean is lift_budget's reference [deg]
    el_center_deg: 23.4             # centre elevation of the path [deg]
    el_height_deg: 14.0             # elevation span of the path, peak to peak [deg]
    downloops: true                 # traversal direction the optimizer solved for
  feasibility:
    min_required: 0.82              # min_feasibility_margin of data/traj_opt.yaml [-]
    turn_radius_headroom: 1.2       # factor on the gate's number for what the GATE adds: its finite-difference curvature estimate and the elevation lift [-]
    turn_radius_request_m: 12.62    # minimum turn radius asked of the optimizer for the LAST request; the gate's margin/(c1*max_steering) times the headroom and the lap's reel-out ratio, since the optimizer measures the radius up the reel-out while the run flies the curve at the anchor [m]
    turn_radius_scale: 1.331        # the two corrections together, as sent [-]
    turn_radius_lap_reelout_m: 0.0  # reel-out per lap ASSUMED for the startup request, the only one with no reply to measure it off [m]
    margin_start: 0.91              # curvature margin at the starting length; the worst case only when one path is flown throughout — see the docstring [-]
    margin_end: 2.31                # curvature margin at reelout_l_max [-]
    c1_final: 0.1919                # turn-rate gain at depower_final [1/m]
    margin_final: 1.6               # curvature margin phase 5 flies with: depower_final's c1, at reelout_l_max, on the STARTING path lifted by el_offset_final [-]
    margin_final_flown: 0.65        # the same, for the last path the re-optimizer installed — the one phase 5 actually inherited [-]
  reopt:
    enabled: true                   # re-optimization during the run
    warm_start: true                # /step alone, seeded from the previous optimum; false re-inits from the parametric guess at every length
    requests: 6                     # solves that completed, accepted or rejected
    blocking: true                  # simulation held while a solve ran
    blocked_s: 73.3                 # wall time the simulation was frozen waiting for replies [s]
    installed: 6                    # new paths actually flown
    blend_retries: 1                # cold-restart attempts spent on a rejected reply — a folded blend, a collapsed prediction, or a clearance/elevation shortfall re-asked at a raised floor — across every install this run
    events:
      t_041.8_s: "installed — margin 1.02 (phase 5: 1.42 at 380 m), clearance 47.4 m, 13796 W predicted" # at L = 189 m
      t_053.6_s: "installed — margin 0.98 (phase 5: 1.17 at 380 m), clearance 51.1 m, 14459 W predicted" # at L = 223 m
      t_065.3_s: "installed — margin 1.01 (phase 5: 1.04 at 380 m), clearance 55.0 m, 14823 W predicted" # at L = 257 m
      t_077.0_s: "installed — margin 0.82 (phase 5: 0.75 at 380 m), clearance 63.6 m, 15058 W predicted" # at L = 291 m
      t_089.2_s: "installed — margin 0.87 (phase 5: 0.70 at 380 m), clearance 72.0 m, 15227 W predicted" # at L = 327 m
      t_105.3_s: "installed — margin 0.93 (droop spread at 50 %, lobe lift at 0 %) (phase 5: 0.65 at 380 m), clearance 83.9 m, 15329 W predicted" # at L = 374 m
  el_bias:
    gain: 0.5                       # per-lap learning gain for the elevation bias; 0 = off
    final_deg: 2.22                 # mean elevation correction added to the last installed path [deg]
    bins: 5                         # el_bias_bins, azimuth bands the correction is learnt over; 1 = one rigid shift
    smooth: 0.25                    # el_bias_smooth, how far each band is pulled towards its neighbours after every lap [-]
    profile_deg: [1.81, 2.14, 2.75, 2.81, 1.59] # the learnt correction per band, crossing first [deg]
    lift_deg: 1.5                   # el_offset_final, the fixed lift added on top of the learnt bias [deg]
    lift_lead_s: 8.0                # el_offset_lead, how early the lift is allowed to latch; 0 = at the end [s]
    wing_deg: 1.5                   # el_offset_wing, extra lift at the lobes, baked into every installed path [deg]
    wing_mode: "azimuth"            # el_offset_wing_mode, the coordinate that lift is shaped over
    wing_az_deg: 10.0               # azimuth mode: azimuth beyond which that lift is full; it ramps over el_offset_wing_blend below it [deg]
    wing_depth: 0.0                 # elevation mode: depth below the path's elevation centre where the lift starts, in half-spans; full at its lowest point [-]
    lift_t_s: 99.3                  # when the lift actually latched [s]
    lift_remaining_m: 23.5          # reel-out left at that moment; > 0 means the lead fired, 0 means phase 5 did [m]
    shift_delivery:
      t_045.8_s: "blended in"       # +0.37° of shift (mean over the bands), curvature margin 1.08 vs 0.82 required
      t_057.6_s: "blended in"       # +0.20° of shift (mean over the bands), curvature margin 0.98 vs 0.82 required
      t_069.3_s: "blended in"       # +0.06° of shift (mean over the bands), curvature margin 0.93 vs 0.82 required
      t_081.0_s: "blended in"       # +0.05° of shift (mean over the bands), curvature margin 0.83 vs 0.82 required
      t_093.3_s: "blended in (100 % of the mean, 75 % of the spread)" # +0.09° of shift (mean over the bands), curvature margin 0.84 vs 0.82 required
      t_097.3_s: "blended in"       # -0.00° of shift (mean over the bands), curvature margin 0.85 vs 0.82 required
      t_101.3_s: "blended in"       # +1.50° of shift (mean over the bands), curvature margin 0.87 vs 0.82 required
      t_105.3_s: "carried by an install (50 % of the spread)" # +0.00° of shift (mean over the bands), curvature margin 0.93 vs 0.82 required
    laps:
      lap_1: -0.73                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.73°, correction became [+0.11 +0.27 +0.44 +0.57 +0.44]° (crossing … lobe)
      lap_2: -0.38                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.74°, correction became [+0.12 +0.34 +0.71 +0.95 +0.70]° (crossing … lobe)
      lap_3: -0.07                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.62°, correction became [-0.01 +0.28 +0.85 +1.18 +0.82]° (crossing … lobe)
      lap_4: -0.04                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.63°, correction became [-0.05 +0.27 +0.96 +1.31 +0.87]° (crossing … lobe)
      lap_5: -0.11                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -0.73°, correction became [-0.01 +0.32 +1.10 +1.47 +0.94]° (crossing … lobe)
      lap_7: -0.63                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -1.31°, correction became [+1.03 +1.27 +1.91 +1.97 +0.96]° (crossing … lobe)
      lap_8: -0.71                  # mean elevation error vs the OPTIMIZER's curve; sag under the flown path -2.06°, correction became [+1.81 +2.14 +2.75 +2.81 +1.59]° (crossing … lobe)
    lap_profiles:
      lap_1: "-0.13 -0.59 -0.89 -1.26 -0.78" # error vs the optimizer's curve per band, crossing first; 193/238/217/183/312 samples, correction now [+0.11 +0.27 +0.44 +0.57 +0.44]° (crossing … lobe)
      lap_2: "+0.08 -0.15 -0.59 -1.00 -0.34" # error vs the optimizer's curve per band, crossing first; 211/213/187/182/270 samples, correction now [+0.12 +0.34 +0.71 +0.95 +0.70]° (crossing … lobe)
      lap_3: "+0.38 +0.14 -0.39 -0.78 +0.04" # error vs the optimizer's curve per band, crossing first; 216/222/190/165/259 samples, correction now [-0.01 +0.28 +0.85 +1.18 +0.82]° (crossing … lobe)
      lap_4: "+0.19 +0.07 -0.36 -0.65 +0.25" # error vs the optimizer's curve per band, crossing first; 220/237/207/142/249 samples, correction now [-0.05 +0.27 +0.96 +1.31 +0.87]° (crossing … lobe)
      lap_5: "+0.03 -0.03 -0.42 -0.73 +0.28" # error vs the optimizer's curve per band, crossing first; 231/257/225/132/255 samples, correction now [-0.01 +0.32 +1.10 +1.47 +0.94]° (crossing … lobe)
      lap_7: "-1.02 -0.94 -0.92 -0.78 +0.34" # error vs the optimizer's curve per band, crossing first; 249/267/194/126/258 samples, correction now [+1.03 +1.27 +1.91 +1.97 +0.96]° (crossing … lobe)
      lap_8: "-0.74 -0.89 -0.95 -1.20 -0.18" # error vs the optimizer's curve per band, crossing first; 237/263/186/128/302 samples, correction now [+1.81 +2.14 +2.75 +2.81 +1.59]° (crossing … lobe)
  droop_profile:
    az_00_20_pct: 2.06              # kite below the path's elevation centre [deg], over 1801 samples; the path itself sits 0.38 half-spans down there and the kite -0.33° under it
    az_20_40_pct: 1.99              # kite below the path's elevation centre [deg], over 1977 samples; the path itself sits 0.29 half-spans down there and the kite -0.73° under it
    az_40_60_pct: 2.0               # kite below the path's elevation centre [deg], over 1698 samples; the path itself sits 0.13 half-spans down there and the kite -1.51° under it
    az_60_80_pct: 2.64              # kite below the path's elevation centre [deg], over 1226 samples; the path itself sits 0.19 half-spans down there and the kite -1.93° under it
    az_80_100_pct: 1.68             # kite below the path's elevation centre [deg], over 2251 samples; the path itself sits 0.25 half-spans down there and the kite -0.79° under it
    centre_to_lobe_deg: 0.58        # how much deeper the kite flies in its worst azimuth band than in the crossing — what a SHAPED lift is aimed at, where el_bias and el_offset_final can only move the pattern rigidly [deg]
  lift_budget:
    delivered_deg: 2.26             # mean correction the path in the air actually carries — el_bias plus el_offset_final once it has been delivered [deg]
    delivered_profile_deg: [1.88, 2.04, 2.43, 2.61, 2.35] # the same, per azimuth band, crossing first [deg]
    commanded_az_amp_deg: 15.88     # mean half-width the run was COMMANDED to fly, against the startup path's in traj_opt.path [deg]
    commanded_el_height_deg: 9.66   # mean commanded pattern height [deg]
    wing_deg: 1.5                   # el_offset_wing, the fixed lobe lift baked into every installed path [deg]
    el_min_run_deg: 9.7             # lowest elevation over the whole run — set in phase 4, so a phase-5 lift does not move it [deg]
    el_min_final_deg: 9.69          # lowest elevation in phase 5, which is what el_offset_final buys [deg]
    azimuth_reach_pos_deg: 4.05     # reach margin: flew 15.16° against the 11.11° required by min_span_frac = 0.70, +36 %
    azimuth_reach_neg_deg: 4.33     # reach margin: flew 15.44° against the 11.11° required by min_span_frac = 0.70, +39 %
    elevation_span_deg: 1.97        # reach margin: flew 8.74° against the 6.77° required by min_span_frac = 0.70, +29 %
    tightest: "elevation span"      # the size criterion with the least room, +1.97° (+29 %) — what the NEXT lift has to spend
  clearance:
    path_min_m: 42.4                # lowest point of the PRE-FLIGHT path at the starting length [m]
    flown_min_m: 45.2               # lowest point the kite actually reached over the run [m]
    min_required_m: 40.0            # min_height of data/traj_opt.yaml [m]
    elevation_min_asked_deg: 9.5    # elevation floor the LAST request carried, the higher of asind(min_height/L) and min_elevation + candidate_elevation_margin at the length it was made for; the optimizer constrains HEIGHT, and only at the far end of the lap's reel-out [deg]
    elevation_min_extra_deg: 0.0    # raise a rejected reply's shortfall added to that floor, carried forward once measured; 0 means no reply was gated out low [deg]
  inflow:
    wind_speed_m_s: 7.0             # wind speed at 6 m sent to the optimizer [m/s]
    wind_direction_deg: 270.0       # direction the wind comes from [deg]
    profile_law: 3                  # 0=CONST, 1=EXP, 2=LOG, 3=EXPLOG, 4-6=CUSTOM_*
  winch:
    k_v: 0.0408                     # v_set = k_v * sqrt(force) sent to the optimizer [-]
    optimize_k_v: false             # k_v sent as a design variable; the reply's value is then the one flown
    k_v_flown: 0.0408               # k_v the run actually flew; == k_v, the optimizer did not move it
    f_min_N: 700.0                  # minimum winch force sent [N]
    f_max_N: 8000.0                 # maximum winch force sent [N]
    force_limit: "soft"             # the reel-out law itself limits the force, inverting the optimizer's own saturating tension curve (beta 1e-03/1e-03, force filtered at tau = 2.48 s); the UpperForceController is held in reset, so upper_force_pct is 0 by construction
performance:
  sim_time_s: 128.6                 # simulated time [s]
  wall_time_s: 110.4                # wall-clock time [s]
  total_wall_time_s: 123.1          # the whole script: package loading, init, settling, the startup solve and the run, up to this summary — the archive copy and any plots follow it [s]
  optimization_time_s: 81.3         # wall time waiting for the optimizer: the startup solve (8.0 s) plus every blocking re-optimization (traj_opt.reopt.blocked_s) [s]
  realtime_factor: 3.47             # sim_time / wall_time, excluding time frozen for re-optimization
  ms_per_step: 3.2                  # wall time per step, excluding time frozen for re-optimization [ms]
  steps: 11571                      # step count
  dt_s: 0.011111111111111112        # simulation timestep [s]
  vsm_interval: 10                  # VSM aerodynamic update interval [steps]
summary:
  date: "2026-08-26"                # wall-clock date the run finished
  time: "12:23:48"                  # wall-clock time the run finished
  wind_speed_gnd: 7.0               # wind speed at 6 m sent to the optimizer [m/s]
  power_ratio: 0.99                 # measured / predicted mean reel-out power, against the weighted prediction
  total_wall_time_s: 123.1          # the whole script, start to this summary [s]
  realtime_factor: 3.47             # sim_time / wall_time, excluding time frozen for re-optimization
  optimization_requests: 6          # solves that completed, accepted or rejected
  optimizations_installed: 6        # new paths actually flown
  av_power_ro: 14412                # mean reel-out power over phase four [W]
  min_power_ro: 345                 # min reel-out power over phase four [W]
  max_power_ro: 16043               # max reel-out power over phase four [W]
  min_force_ro: 4132                # min tether force over phase four [N]
  av_force_ro: 5068                 # mean tether force over phase four [N]
  max_force_ro: 5833                # max tether force over phase four [N]
  v_ro_min: 0.07                    # min reel-out speed over phase four [m/s]
  v_ro_av: 2.84                     # mean reel-out speed over phase four [m/s]
  v_ro_max: 3.06                    # max reel-out speed over phase four [m/s]
  av_depower_ro: 0.27               # mean KCU depower over phase four [-]
